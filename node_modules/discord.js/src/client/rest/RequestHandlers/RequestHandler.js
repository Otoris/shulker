/**
 * A base class for different types of rate limiting handlers for the REST API.
 * @private
 */
class RequestHandler ***REMOVED***
  /**
   * @param ***REMOVED***RESTManager***REMOVED*** restManager The REST manager to use
   */
  constructor(restManager) ***REMOVED***
    /**
     * The RESTManager that instantiated this RequestHandler
     * @type ***REMOVED***RESTManager***REMOVED***
     */
    this.restManager = restManager;

    /**
     * A list of requests that have yet to be processed
     * @type ***REMOVED***APIRequest[]***REMOVED***
     */
    this.queue = [];
  ***REMOVED***

  /**
   * Whether or not the client is being rate limited on every endpoint
   * @type ***REMOVED***boolean***REMOVED***
   * @readonly
   */
  get globalLimit() ***REMOVED***
    return this.restManager.globallyRateLimited;
  ***REMOVED***

  set globalLimit(value) ***REMOVED***
    this.restManager.globallyRateLimited = value;
  ***REMOVED***

  /**
   * Push a new API request into this bucket.
   * @param ***REMOVED***APIRequest***REMOVED*** request The new request to push into the queue
   */
  push(request) ***REMOVED***
    this.queue.push(request);
  ***REMOVED***

  /**
   * Attempts to get this RequestHandler to process its current queue.
   */
  handle() ***REMOVED******REMOVED*** // eslint-disable-line no-empty-function

  destroy() ***REMOVED***
    this.queue = [];
  ***REMOVED***
***REMOVED***

module.exports = RequestHandler;

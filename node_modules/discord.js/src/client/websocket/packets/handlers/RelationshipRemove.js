const AbstractHandler = require('./AbstractHandler');

class RelationshipRemoveHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    if (data.type === 2) ***REMOVED***
      if (client.user.blocked.has(data.id)) ***REMOVED***
        client.user.blocked.delete(data.id);
      ***REMOVED***
    ***REMOVED*** else if (data.type === 1) ***REMOVED***
      if (client.user.friends.has(data.id)) ***REMOVED***
        client.user.friends.delete(data.id);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
***REMOVED***

module.exports = RelationshipRemoveHandler;

const AbstractHandler = require('./AbstractHandler');
const Constants = require('../../../../util/Constants');

class GuildDeleteHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    const response = client.actions.GuildDelete.handle(data);
    if (response.guild) client.emit(Constants.Events.GUILD_DELETE, response.guild);
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a guild is deleted/left.
 * @event Client#guildDelete
 * @param ***REMOVED***Guild***REMOVED*** guild The guild that was deleted
 */

module.exports = GuildDeleteHandler;

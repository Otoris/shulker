const AbstractHandler = require('./AbstractHandler');

class MessageDeleteBulkHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    client.actions.MessageDeleteBulk.handle(data);
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever messages are deleted in bulk.
 * @event Client#messageDeleteBulk
 * @param ***REMOVED***Collection<Snowflake, Message>***REMOVED*** messages The deleted messages, mapped by their ID
 */

module.exports = MessageDeleteBulkHandler;

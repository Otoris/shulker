// ##untested handler##

const AbstractHandler = require('./AbstractHandler');

class GuildBanRemoveHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    client.actions.GuildBanRemove.handle(data);
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a member is unbanned from a guild.
 * @event Client#guildBanRemove
 * @param ***REMOVED***Guild***REMOVED*** guild The guild that the unban occurred in
 * @param ***REMOVED***User***REMOVED*** user The user that was unbanned
 */

module.exports = GuildBanRemoveHandler;

const AbstractHandler = require('./AbstractHandler');

class RelationshipAddHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    if (data.type === 1) ***REMOVED***
      client.fetchUser(data.id).then(user => ***REMOVED***
        client.user.friends.set(user.id, user);
      ***REMOVED***);
    ***REMOVED*** else if (data.type === 2) ***REMOVED***
      client.fetchUser(data.id).then(user => ***REMOVED***
        client.user.blocked.set(user.id, user);
      ***REMOVED***);
    ***REMOVED***
  ***REMOVED***
***REMOVED***

module.exports = RelationshipAddHandler;

const AbstractHandler = require('./AbstractHandler');
const ***REMOVED*** Events ***REMOVED*** = require('../../../../util/Constants');

class GuildIntegrationsHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    const guild = client.guilds.get(data.guild_id);
    if (guild) client.emit(Events.GUILD_INTEGRATIONS_UPDATE, guild);
  ***REMOVED***
***REMOVED***

module.exports = GuildIntegrationsHandler;

/**
 * Emitted whenever a guild integration is updated
 * @event Client#guildIntegrationsUpdate
 * @param ***REMOVED***Guild***REMOVED*** guild The guild whose integrations were updated
 */

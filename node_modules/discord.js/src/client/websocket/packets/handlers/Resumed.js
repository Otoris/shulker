const AbstractHandler = require('./AbstractHandler');
const Constants = require('../../../../util/Constants');

class ResumedHandler extends AbstractHandler ***REMOVED***
  handle() ***REMOVED***
    const client = this.packetManager.client;
    const ws = client.ws.connection;

    ws.status = Constants.Status.READY;
    this.packetManager.handleQueue();

    const replayed = ws.sequence - ws.closeSequence;

    ws.debug(`RESUMED | replayed $***REMOVED***replayed***REMOVED*** events.`);
    client.emit(Constants.Events.RESUME, replayed);
    ws.heartbeat();
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a WebSocket resumes.
 * @event Client#resume
 * @param ***REMOVED***number***REMOVED*** replayed The number of events that were replayed
 */

module.exports = ResumedHandler;

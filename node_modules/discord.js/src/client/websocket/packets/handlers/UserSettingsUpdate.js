const AbstractHandler = require('./AbstractHandler');
const Constants = require('../../../../util/Constants');

class UserSettingsUpdateHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    client.user.settings.patch(packet.d);
    client.emit(Constants.Events.USER_SETTINGS_UPDATE, client.user.settings);
  ***REMOVED***
***REMOVED***

/**
 * Emitted when the client user's settings update.
 * @event Client#clientUserSettingsUpdate
 * @param ***REMOVED***ClientUserSettings***REMOVED*** clientUserSettings The new client user settings
 */

module.exports = UserSettingsUpdateHandler;

const AbstractHandler = require('./AbstractHandler');
const Constants = require('../../../../util/Constants');

class MessageCreateHandler extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    const response = client.actions.MessageCreate.handle(data);
    if (response.message) client.emit(Constants.Events.MESSAGE_CREATE, response.message);
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a message is created.
 * @event Client#message
 * @param ***REMOVED***Message***REMOVED*** message The created message
 */

module.exports = MessageCreateHandler;

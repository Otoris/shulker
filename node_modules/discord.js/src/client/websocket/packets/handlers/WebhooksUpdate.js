const AbstractHandler = require('./AbstractHandler');
const ***REMOVED*** Events ***REMOVED*** = require('../../../../util/Constants');

class WebhooksUpdate extends AbstractHandler ***REMOVED***
  handle(packet) ***REMOVED***
    const client = this.packetManager.client;
    const data = packet.d;
    const channel = client.channels.get(data.channel_id);
    if (channel) client.emit(Events.WEBHOOKS_UPDATE, channel);
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a guild text channel has its webhooks changed.
 * @event Client#webhookUpdate
 * @param ***REMOVED***TextChannel***REMOVED*** channel The channel that had a webhook update
 */

module.exports = WebhooksUpdate;

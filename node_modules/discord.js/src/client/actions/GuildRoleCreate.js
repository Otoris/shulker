const Action = require('./Action');
const Constants = require('../../util/Constants');
const Role = require('../../structures/Role');

class GuildRoleCreate extends Action ***REMOVED***
  handle(data) ***REMOVED***
    const client = this.client;
    const guild = client.guilds.get(data.guild_id);
    let role;
    if (guild) ***REMOVED***
      const already = guild.roles.has(data.role.id);
      role = new Role(guild, data.role);
      guild.roles.set(role.id, role);
      if (!already) client.emit(Constants.Events.GUILD_ROLE_CREATE, role);
    ***REMOVED***
    return ***REMOVED*** role ***REMOVED***;
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a role is created.
 * @event Client#roleCreate
 * @param ***REMOVED***Role***REMOVED*** role The role that was created
 */

module.exports = GuildRoleCreate;

const Action = require('./Action');
const Constants = require('../../util/Constants');
const Util = require('../../util/Util');

class GuildUpdateAction extends Action ***REMOVED***
  handle(data) ***REMOVED***
    const client = this.client;

    const guild = client.guilds.get(data.id);
    if (guild) ***REMOVED***
      const oldGuild = Util.cloneObject(guild);
      guild.setup(data);
      client.emit(Constants.Events.GUILD_UPDATE, oldGuild, guild);
      return ***REMOVED***
        old: oldGuild,
        updated: guild,
      ***REMOVED***;
    ***REMOVED***

    return ***REMOVED***
      old: null,
      updated: null,
    ***REMOVED***;
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a guild is updated - e.g. name change.
 * @event Client#guildUpdate
 * @param ***REMOVED***Guild***REMOVED*** oldGuild The guild before the update
 * @param ***REMOVED***Guild***REMOVED*** newGuild The guild after the update
 */

module.exports = GuildUpdateAction;

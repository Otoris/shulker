const Action = require('./Action');

class GuildEmojiCreateAction extends Action ***REMOVED***
  handle(guild, createdEmoji) ***REMOVED***
    const client = this.client;
    const emoji = client.dataManager.newEmoji(createdEmoji, guild);
    return ***REMOVED*** emoji ***REMOVED***;
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a custom emoji is created in a guild.
 * @event Client#emojiCreate
 * @param ***REMOVED***Emoji***REMOVED*** emoji The emoji that was created
 */

module.exports = GuildEmojiCreateAction;

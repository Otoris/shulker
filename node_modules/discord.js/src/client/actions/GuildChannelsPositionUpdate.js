const Action = require('./Action');

class GuildChannelsPositionUpdate extends Action ***REMOVED***
  handle(data) ***REMOVED***
    const client = this.client;

    const guild = client.guilds.get(data.guild_id);
    if (guild) ***REMOVED***
      for (const partialChannel of data.channels) ***REMOVED***
        const channel = guild.channels.get(partialChannel.id);
        if (channel) channel.position = partialChannel.position;
      ***REMOVED***
    ***REMOVED***

    return ***REMOVED*** guild ***REMOVED***;
  ***REMOVED***
***REMOVED***

module.exports = GuildChannelsPositionUpdate;

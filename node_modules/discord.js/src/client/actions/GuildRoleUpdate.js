const Action = require('./Action');
const Constants = require('../../util/Constants');
const Util = require('../../util/Util');

class GuildRoleUpdateAction extends Action ***REMOVED***
  handle(data) ***REMOVED***
    const client = this.client;
    const guild = client.guilds.get(data.guild_id);

    if (guild) ***REMOVED***
      const roleData = data.role;
      let oldRole = null;

      const role = guild.roles.get(roleData.id);
      if (role) ***REMOVED***
        oldRole = Util.cloneObject(role);
        role.setup(data.role);
        client.emit(Constants.Events.GUILD_ROLE_UPDATE, oldRole, role);
      ***REMOVED***

      return ***REMOVED***
        old: oldRole,
        updated: role,
      ***REMOVED***;
    ***REMOVED***

    return ***REMOVED***
      old: null,
      updated: null,
    ***REMOVED***;
  ***REMOVED***
***REMOVED***

/**
 * Emitted whenever a guild role is updated.
 * @event Client#roleUpdate
 * @param ***REMOVED***Role***REMOVED*** oldRole The role before the update
 * @param ***REMOVED***Role***REMOVED*** newRole The role after the update
 */

module.exports = GuildRoleUpdateAction;

const GuildChannel = require('./GuildChannel');

/**
 * Represents a guild category channel on Discord.
 * @extends ***REMOVED***GuildChannel***REMOVED***
 */
class CategoryChannel extends GuildChannel ***REMOVED***
  constructor(guild, data) ***REMOVED***
    super(guild, data);
    this.type = 'category';
  ***REMOVED***
  /**
   * The channels that are part of this category
   * @type ***REMOVED***?Collection<Snowflake, GuildChannel>***REMOVED***
   * @readonly
   */
  get children() ***REMOVED***
    return this.guild.channels.filter(c => c.parentID === this.id);
  ***REMOVED***
***REMOVED***

module.exports = CategoryChannel;

const Constants = require('../util/Constants');

/**
 * A wrapper around the ClientUser's channel overrides.
 */
class ClientUserChannelOverride ***REMOVED***
  constructor(data) ***REMOVED***
    this.patch(data);
  ***REMOVED***

  /**
   * Patch the data contained in this class with new partial data.
   * @param ***REMOVED***Object***REMOVED*** data Data to patch this with
   * @returns ***REMOVED***void***REMOVED***
   * @private
   */
  patch(data) ***REMOVED***
    for (const key of Object.keys(Constants.UserChannelOverrideMap)) ***REMOVED***
      const value = Constants.UserChannelOverrideMap[key];
      if (!data.hasOwnProperty(key)) continue;
      if (typeof value === 'function') ***REMOVED***
        this[value.name] = value(data[key]);
      ***REMOVED*** else ***REMOVED***
        this[value] = data[key];
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
***REMOVED***

module.exports = ClientUserChannelOverride;

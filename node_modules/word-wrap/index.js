/*!
 * word-wrap <https://github.com/jonschlinkert/word-wrap>
 *
 * Copyright (c) 2014-2017, Jon Schlinkert.
 * Released under the MIT License.
 */

module.exports = function(str, options) ***REMOVED***
  options = options || ***REMOVED******REMOVED***;
  if (str == null) ***REMOVED***
    return str;
  ***REMOVED***

  var width = options.width || 50;
  var indent = (typeof options.indent === 'string')
    ? options.indent
    : '  ';

  var newline = options.newline || '\n' + indent;
  var escape = typeof options.escape === 'function'
    ? options.escape
    : identity;

  var regexString = '.***REMOVED***1,' + width + '***REMOVED***';
  if (options.cut !== true) ***REMOVED***
    regexString += '([\\s\u200B]+|$)|[^\\s\u200B]+?([\\s\u200B]+|$)';
  ***REMOVED***

  var re = new RegExp(regexString, 'g');
  var lines = str.match(re) || [];
  var result = indent + lines.map(function(line) ***REMOVED***
    if (line.slice(-1) === '\n') ***REMOVED***
      line = line.slice(0, line.length - 1);
    ***REMOVED***
    return escape(line);
  ***REMOVED***).join(newline);

  if (options.trim === true) ***REMOVED***
    result = result.replace(/[ \t]*$/gm, '');
  ***REMOVED***
  return result;
***REMOVED***;

function identity(str) ***REMOVED***
  return str;
***REMOVED***

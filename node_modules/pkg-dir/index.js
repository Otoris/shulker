'use strict';
const path = require('path');
const findUp = require('find-up');

module.exports = cwd => findUp('package.json', ***REMOVED***cwd***REMOVED***).then(fp => fp ? path.dirname(fp) : null);

module.exports.sync = cwd => ***REMOVED***
	const fp = findUp.sync('package.json', ***REMOVED***cwd***REMOVED***);
	return fp ? path.dirname(fp) : null;
***REMOVED***;

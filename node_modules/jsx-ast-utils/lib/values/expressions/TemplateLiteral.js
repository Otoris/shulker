'use strict';

Object.defineProperty(exports, "__esModule", ***REMOVED***
  value: true
***REMOVED***);
exports.default = extractValueFromTemplateLiteral;
/**
 * Returns the string value of a template literal object.
 * Tries to build it as best as it can based on the passed
 * prop. For instance `This is a $***REMOVED***prop***REMOVED***` will return 'This is a ***REMOVED***prop***REMOVED***'.
 *
 * If the template literal builds to undefined (`$***REMOVED***undefined***REMOVED***`), then
 * this should return "undefined".
 */
function extractValueFromTemplateLiteral(value) ***REMOVED***
  var quasis = value.quasis,
      expressions = value.expressions;

  var partitions = quasis.concat(expressions);

  return partitions.sort(function (a, b) ***REMOVED***
    return a.start - b.start;
  ***REMOVED***).reduce(function (raw, part) ***REMOVED***
    var type = part.type;

    if (type === 'TemplateElement') ***REMOVED***
      return raw + part.value.raw;
    ***REMOVED***

    if (type === 'Identifier') ***REMOVED***
      return part.name === 'undefined' ? '' + raw + part.name : raw + '***REMOVED***' + part.name + '***REMOVED***';
    ***REMOVED***

    if (type.indexOf('Expression') > -1) ***REMOVED***
      return raw + '***REMOVED***' + type + '***REMOVED***';
    ***REMOVED***

    return raw;
  ***REMOVED***, '');
***REMOVED***
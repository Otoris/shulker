const JS_RESERVED = ***REMOVED***
  Array,
  Date,
  Infinity,
  Math,
  Number,
  Object,
  String,
  undefined,
***REMOVED***;

/**
 * Extractor function for a Identifier type value node.
 * An Identifier is usually a reference to a variable.
 * Just return variable name to determine its existence.
 *
 * @param - value - AST Value object with type `Identifier`
 * @returns - The extracted value converted to correct type.
 */
export default function extractValueFromIdentifier(value) ***REMOVED***
  const ***REMOVED*** name ***REMOVED*** = value;

  if (Object.hasOwnProperty.call(JS_RESERVED, name)) ***REMOVED***
    return JS_RESERVED[name];
  ***REMOVED***

  return name;
***REMOVED***

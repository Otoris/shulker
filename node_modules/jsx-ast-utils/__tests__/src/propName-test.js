/* eslint-env mocha */
import assert from 'assert';
import ***REMOVED*** extractProp, setParserName ***REMOVED*** from '../helper';
import propName from '../../src/propName';

describe('propName', () => ***REMOVED***
  beforeEach(() => ***REMOVED***
    setParserName('babel');
  ***REMOVED***);
  it('should export a function', () => ***REMOVED***
    const expected = 'function';
    const actual = typeof propName;

    assert.equal(expected, actual);
  ***REMOVED***);

  it('should throw an error if the argument is missing', () => ***REMOVED***
    assert.throws(() => ***REMOVED*** propName(); ***REMOVED***, Error);
  ***REMOVED***);

  it('should throw an error if the argument not a JSX node', () => ***REMOVED***
    assert.throws(() => ***REMOVED*** propName(***REMOVED*** a: 'foo' ***REMOVED***); ***REMOVED***, Error);
  ***REMOVED***);

  it('should return correct name for normal prop', () => ***REMOVED***
    const prop = extractProp('<div foo="bar" />');

    const expected = 'foo';
    const actual = propName(prop);

    assert.equal(expected, actual);
  ***REMOVED***);

  it('should return correct name for namespaced prop', () => ***REMOVED***
    const prop = extractProp('<div foo:bar="baz" />', 'foo:bar');

    const expected = 'foo:bar';
    const actual = propName(prop);

    assert.equal(expected, actual);
  ***REMOVED***);
***REMOVED***);

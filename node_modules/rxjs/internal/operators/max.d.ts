import ***REMOVED*** MonoTypeOperatorFunction ***REMOVED*** from '../types';
/**
 * The Max operator operates on an Observable that emits numbers (or items that can be compared with a provided function),
 * and when source Observable completes it emits a single item: the item with the largest value.
 *
 * ![](max.png)
 *
 * ## Examples
 * Get the maximal value of a series of numbers
 * ```ts
 * import ***REMOVED*** of ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** max ***REMOVED*** from 'rxjs/operators';
 *
 * of(5, 4, 7, 2, 8).pipe(
 *   max(),
 * )
 * .subscribe(x => console.log(x)); // -> 8
 * ```
 *
 * Use a comparer function to get the maximal item
 * ```typescript
 * import ***REMOVED*** of ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** max ***REMOVED*** from 'rxjs/operators';
 *
 * interface Person ***REMOVED***
 *   age: number,
 *   name: string
 * ***REMOVED***
 * of<Person>(
 *   ***REMOVED***age: 7, name: 'Foo'***REMOVED***,
 *   ***REMOVED***age: 5, name: 'Bar'***REMOVED***,
 *   ***REMOVED***age: 9, name: 'Beer'***REMOVED***,
 * ).pipe(
 *   max<Person>((a: Person, b: Person) => a.age < b.age ? -1 : 1),
 * )
 * .subscribe((x: Person) => console.log(x.name)); // -> 'Beer'
 * ```
 *
 * @see ***REMOVED***@link min***REMOVED***
 *
 * @param ***REMOVED***Function***REMOVED*** [comparer] - Optional comparer function that it will use instead of its default to compare the
 * value of two items.
 * @return ***REMOVED***Observable***REMOVED*** An Observable that emits item with the largest value.
 * @method max
 * @owner Observable
 */
export declare function max<T>(comparer?: (x: T, y: T) => number): MonoTypeOperatorFunction<T>;

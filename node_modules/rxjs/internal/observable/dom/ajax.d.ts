import ***REMOVED*** AjaxCreationMethod ***REMOVED*** from './AjaxObservable';
/**
 * There is an ajax operator on the Rx object.
 *
 * It creates an observable for an Ajax request with either a request object with
 * url, headers, etc or a string for a URL.
 *
 *
 * ## Using ajax() to fetch the response object that is being returned from API.
 * ```ts
 * import ***REMOVED*** ajax ***REMOVED*** from 'rxjs/ajax';
 * import ***REMOVED*** map, catchError ***REMOVED*** from 'rxjs/operators';
 * import ***REMOVED*** of ***REMOVED*** from 'rxjs';
 *
 * const obs$ = ajax(`https://api.github.com/users?per_page=5`).pipe(
 *   map(userResponse => console.log('users: ', userResponse)),
 *   catchError(error => ***REMOVED***
 *     console.log('error: ', error);
 *     return of(error);
 *   ***REMOVED***)
 * );
 *
 * ```
 *
 * ## Using ajax.getJSON() to fetch data from API.
 * ```ts
 * import ***REMOVED*** ajax ***REMOVED*** from 'rxjs/ajax';
 * import ***REMOVED*** map, catchError ***REMOVED*** from 'rxjs/operators';
 * import ***REMOVED*** of ***REMOVED*** from 'rxjs';
 *
 * const obs$ = ajax.getJSON(`https://api.github.com/users?per_page=5`).pipe(
 *   map(userResponse => console.log('users: ', userResponse)),
 *   catchError(error => ***REMOVED***
 *     console.log('error: ', error);
 *     return of(error);
 *   ***REMOVED***)
 * );
 *
 * ```
 *
 * ## Using ajax() with object as argument and method POST with a two seconds delay.
 * ```ts
 * import ***REMOVED*** ajax ***REMOVED*** from 'rxjs/ajax';
 * import ***REMOVED*** of ***REMOVED*** from 'rxjs';
 *
 * const users = ajax(***REMOVED***
 *   url: 'https://httpbin.org/delay/2',
 *   method: 'POST',
 *   headers: ***REMOVED***
 *     'Content-Type': 'application/json',
 *     'rxjs-custom-header': 'Rxjs'
 *   ***REMOVED***,
 *   body: ***REMOVED***
 *     rxjs: 'Hello World!'
 *   ***REMOVED***
 * ***REMOVED***).pipe(
 *   map(response => console.log('response: ', response)),
 *   catchError(error => ***REMOVED***
 *     console.log('error: ', error);
 *     return of(error);
 *   ***REMOVED***)
 * );
 *
 * ```
 *
 * ## Using ajax() to fetch. An error object that is being returned from the request.
 * ```ts
 * import ***REMOVED*** ajax ***REMOVED*** from 'rxjs/ajax';
 * import ***REMOVED*** map, catchError ***REMOVED*** from 'rxjs/operators';
 * import ***REMOVED*** of ***REMOVED*** from 'rxjs';
 *
 * const obs$ = ajax(`https://api.github.com/404`).pipe(
 *   map(userResponse => console.log('users: ', userResponse)),
 *   catchError(error => ***REMOVED***
 *     console.log('error: ', error);
 *     return of(error);
 *   ***REMOVED***)
 * );
 *
 * ```
 */
export declare const ajax: AjaxCreationMethod;

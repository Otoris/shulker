import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** SchedulerLike ***REMOVED*** from '../types';
/**
 * Creates an Observable that emits no items to the Observer and immediately
 * emits an error notification.
 *
 * <span class="informal">Just emits 'error', and nothing else.
 * </span>
 *
 * ![](throw.png)
 *
 * This static operator is useful for creating a simple Observable that only
 * emits the error notification. It can be used for composing with other
 * Observables, such as in a ***REMOVED***@link mergeMap***REMOVED***.
 *
 * ## Examples
 * ### Emit the number 7, then emit an error
 * ```ts
 * import ***REMOVED*** throwError, concat, of ***REMOVED*** from 'rxjs';
 *
 * const result = concat(of(7), throwError(new Error('oops!')));
 * result.subscribe(x => console.log(x), e => console.error(e));
 *
 * // Logs:
 * // 7
 * // Error: oops!
 * ```
 *
 * ---
 *
 * ### Map and flatten numbers to the sequence 'a', 'b', 'c', but throw an error for 2
 * ```ts
 * import ***REMOVED*** throwError, interval, of ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** mergeMap ***REMOVED*** from 'rxjs/operators';
 *
 * interval(1000).pipe(
 *   mergeMap(x => x === 2
 *     ? throwError('Twos are bad')
 *     : of('a', 'b', 'c')
 *   ),
 * ).subscribe(x => console.log(x), e => console.error(e));
 *
 * // Logs:
 * // a
 * // b
 * // c
 * // a
 * // b
 * // c
 * // Twos are bad
 * ```
 *
 * @see ***REMOVED***@link Observable***REMOVED***
 * @see ***REMOVED***@link empty***REMOVED***
 * @see ***REMOVED***@link never***REMOVED***
 * @see ***REMOVED***@link of***REMOVED***
 *
 * @param ***REMOVED***any***REMOVED*** error The particular Error to pass to the error notification.
 * @param ***REMOVED***SchedulerLike***REMOVED*** [scheduler] A ***REMOVED***@link SchedulerLike***REMOVED*** to use for scheduling
 * the emission of the error notification.
 * @return ***REMOVED***Observable***REMOVED*** An error Observable: emits only the error notification
 * using the given error argument.
 * @static true
 * @name throwError
 * @owner Observable
 */
export declare function throwError(error: any, scheduler?: SchedulerLike): Observable<never>;

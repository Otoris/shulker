import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** SchedulerLike ***REMOVED*** from '../types';
import ***REMOVED*** subscribeToPromise ***REMOVED*** from '../util/subscribeToPromise';
import ***REMOVED*** schedulePromise ***REMOVED*** from '../scheduled/schedulePromise';

export function fromPromise<T>(input: PromiseLike<T>, scheduler?: SchedulerLike) ***REMOVED***
  if (!scheduler) ***REMOVED***
    return new Observable<T>(subscribeToPromise(input));
  ***REMOVED*** else ***REMOVED***
    return schedulePromise(input, scheduler);
  ***REMOVED***
***REMOVED***

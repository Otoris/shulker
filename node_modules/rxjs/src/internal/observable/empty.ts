import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** SchedulerLike ***REMOVED*** from '../types';

/**
 * The same Observable instance returned by any call to ***REMOVED***@link empty***REMOVED*** without a
 * `scheduler`. It is preferrable to use this over `empty()`.
 */
export const EMPTY = new Observable<never>(subscriber => subscriber.complete());

/**
 * Creates an Observable that emits no items to the Observer and immediately
 * emits a complete notification.
 *
 * <span class="informal">Just emits 'complete', and nothing else.
 * </span>
 *
 * ![](empty.png)
 *
 * This static operator is useful for creating a simple Observable that only
 * emits the complete notification. It can be used for composing with other
 * Observables, such as in a ***REMOVED***@link mergeMap***REMOVED***.
 *
 * ## Examples
 * ### Emit the number 7, then complete
 * ```ts
 * import ***REMOVED*** empty ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** startWith ***REMOVED*** from 'rxjs/operators';
 *
 * const result = empty().pipe(startWith(7));
 * result.subscribe(x => console.log(x));
 * ```
 *
 * ### Map and flatten only odd numbers to the sequence 'a', 'b', 'c'
 * ```ts
 * import ***REMOVED*** empty, interval, of ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** mergeMap ***REMOVED*** from 'rxjs/operators';
 *
 * const interval$ = interval(1000);
 * const result = interval$.pipe(
 *   mergeMap(x => x % 2 === 1 ? of('a', 'b', 'c') : empty()),
 * );
 * result.subscribe(x => console.log(x));
 *
 * // Results in the following to the console:
 * // x is equal to the count on the interval eg(0,1,2,3,...)
 * // x will occur every 1000ms
 * // if x % 2 is equal to 1 print abc
 * // if x % 2 is not equal to 1 nothing will be output
 * ```
 *
 * @see ***REMOVED***@link Observable***REMOVED***
 * @see ***REMOVED***@link never***REMOVED***
 * @see ***REMOVED***@link of***REMOVED***
 * @see ***REMOVED***@link throwError***REMOVED***
 *
 * @param scheduler A ***REMOVED***@link SchedulerLike***REMOVED*** to use for scheduling
 * the emission of the complete notification.
 * @return An "empty" Observable: emits only the complete
 * notification.
 * @deprecated Deprecated in favor of using ***REMOVED***@link EMPTY***REMOVED*** constant, or ***REMOVED***@link scheduled***REMOVED*** (e.g. `scheduled([], scheduler)`)
 */
export function empty(scheduler?: SchedulerLike) ***REMOVED***
  return scheduler ? emptyScheduled(scheduler) : EMPTY;
***REMOVED***

function emptyScheduled(scheduler: SchedulerLike) ***REMOVED***
  return new Observable<never>(subscriber => scheduler.schedule(() => subscriber.complete()));
***REMOVED***

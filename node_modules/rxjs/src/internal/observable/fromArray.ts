import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** SchedulerLike ***REMOVED*** from '../types';
import ***REMOVED*** subscribeToArray ***REMOVED*** from '../util/subscribeToArray';
import ***REMOVED*** scheduleArray ***REMOVED*** from '../scheduled/scheduleArray';

export function fromArray<T>(input: ArrayLike<T>, scheduler?: SchedulerLike) ***REMOVED***
  if (!scheduler) ***REMOVED***
    return new Observable<T>(subscribeToArray(input));
  ***REMOVED*** else ***REMOVED***
    return scheduleArray(input, scheduler);
  ***REMOVED***
***REMOVED***

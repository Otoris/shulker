import ***REMOVED*** Scheduler ***REMOVED*** from '../Scheduler';
import ***REMOVED*** SubscriptionLog ***REMOVED*** from './SubscriptionLog';

export class SubscriptionLoggable ***REMOVED***
  public subscriptions: SubscriptionLog[] = [];
  scheduler: Scheduler;

  logSubscribedFrame(): number ***REMOVED***
    this.subscriptions.push(new SubscriptionLog(this.scheduler.now()));
    return this.subscriptions.length - 1;
  ***REMOVED***

  logUnsubscribedFrame(index: number) ***REMOVED***
    const subscriptionLogs = this.subscriptions;
    const oldSubscriptionLog = subscriptionLogs[index];
    subscriptionLogs[index] = new SubscriptionLog(
      oldSubscriptionLog.subscribedFrame,
      this.scheduler.now()
    );
  ***REMOVED***
***REMOVED***

import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** Operator ***REMOVED*** from '../Operator';
import ***REMOVED*** Subscriber ***REMOVED*** from '../Subscriber';
import ***REMOVED*** Subscription ***REMOVED*** from '../Subscription';
import ***REMOVED*** Notification ***REMOVED*** from '../Notification';
import ***REMOVED*** MonoTypeOperatorFunction, PartialObserver, SchedulerAction, SchedulerLike, TeardownLogic ***REMOVED*** from '../types';

/**
 *
 * Re-emits all notifications from source Observable with specified scheduler.
 *
 * <span class="informal">Ensure a specific scheduler is used, from outside of an Observable.</span>
 *
 * `observeOn` is an operator that accepts a scheduler as a first parameter, which will be used to reschedule
 * notifications emitted by the source Observable. It might be useful, if you do not have control over
 * internal scheduler of a given Observable, but want to control when its values are emitted nevertheless.
 *
 * Returned Observable emits the same notifications (nexted values, complete and error events) as the source Observable,
 * but rescheduled with provided scheduler. Note that this doesn't mean that source Observables internal
 * scheduler will be replaced in any way. Original scheduler still will be used, but when the source Observable emits
 * notification, it will be immediately scheduled again - this time with scheduler passed to `observeOn`.
 * An anti-pattern would be calling `observeOn` on Observable that emits lots of values synchronously, to split
 * that emissions into asynchronous chunks. For this to happen, scheduler would have to be passed into the source
 * Observable directly (usually into the operator that creates it). `observeOn` simply delays notifications a
 * little bit more, to ensure that they are emitted at expected moments.
 *
 * As a matter of fact, `observeOn` accepts second parameter, which specifies in milliseconds with what delay notifications
 * will be emitted. The main difference between ***REMOVED***@link delay***REMOVED*** operator and `observeOn` is that `observeOn`
 * will delay all notifications - including error notifications - while `delay` will pass through error
 * from source Observable immediately when it is emitted. In general it is highly recommended to use `delay` operator
 * for any kind of delaying of values in the stream, while using `observeOn` to specify which scheduler should be used
 * for notification emissions in general.
 *
 * ## Example
 * Ensure values in subscribe are called just before browser repaint.
 * ```ts
 * import ***REMOVED*** interval ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** observeOn ***REMOVED*** from 'rxjs/operators';
 *
 * const intervals = interval(10);                // Intervals are scheduled
 *                                                // with async scheduler by default...
 * intervals.pipe(
 *   observeOn(animationFrameScheduler),          // ...but we will observe on animationFrame
 * )                                              // scheduler to ensure smooth animation.
 * .subscribe(val => ***REMOVED***
 *   someDiv.style.height = val + 'px';
 * ***REMOVED***);
 * ```
 *
 * @see ***REMOVED***@link delay***REMOVED***
 *
 * @param ***REMOVED***SchedulerLike***REMOVED*** scheduler Scheduler that will be used to reschedule notifications from source Observable.
 * @param ***REMOVED***number***REMOVED*** [delay] Number of milliseconds that states with what delay every notification should be rescheduled.
 * @return ***REMOVED***Observable<T>***REMOVED*** Observable that emits the same notifications as the source Observable,
 * but with provided scheduler.
 *
 * @method observeOn
 * @owner Observable
 */
export function observeOn<T>(scheduler: SchedulerLike, delay: number = 0): MonoTypeOperatorFunction<T> ***REMOVED***
  return function observeOnOperatorFunction(source: Observable<T>): Observable<T> ***REMOVED***
    return source.lift(new ObserveOnOperator(scheduler, delay));
  ***REMOVED***;
***REMOVED***

export class ObserveOnOperator<T> implements Operator<T, T> ***REMOVED***
  constructor(private scheduler: SchedulerLike, private delay: number = 0) ***REMOVED***
  ***REMOVED***

  call(subscriber: Subscriber<T>, source: any): TeardownLogic ***REMOVED***
    return source.subscribe(new ObserveOnSubscriber(subscriber, this.scheduler, this.delay));
  ***REMOVED***
***REMOVED***

/**
 * We need this JSDoc comment for affecting ESDoc.
 * @ignore
 * @extends ***REMOVED***Ignored***REMOVED***
 */
export class ObserveOnSubscriber<T> extends Subscriber<T> ***REMOVED***
  /** @nocollapse */
  static dispatch(this: SchedulerAction<ObserveOnMessage>, arg: ObserveOnMessage) ***REMOVED***
    const ***REMOVED*** notification, destination ***REMOVED*** = arg;
    notification.observe(destination);
    this.unsubscribe();
  ***REMOVED***

  constructor(destination: Subscriber<T>,
              private scheduler: SchedulerLike,
              private delay: number = 0) ***REMOVED***
    super(destination);
  ***REMOVED***

  private scheduleMessage(notification: Notification<any>): void ***REMOVED***
    const destination = this.destination as Subscription;
    destination.add(this.scheduler.schedule(
      ObserveOnSubscriber.dispatch,
      this.delay,
      new ObserveOnMessage(notification, this.destination)
    ));
  ***REMOVED***

  protected _next(value: T): void ***REMOVED***
    this.scheduleMessage(Notification.createNext(value));
  ***REMOVED***

  protected _error(err: any): void ***REMOVED***
    this.scheduleMessage(Notification.createError(err));
    this.unsubscribe();
  ***REMOVED***

  protected _complete(): void ***REMOVED***
    this.scheduleMessage(Notification.createComplete());
    this.unsubscribe();
  ***REMOVED***
***REMOVED***

export class ObserveOnMessage ***REMOVED***
  constructor(public notification: Notification<any>,
              public destination: PartialObserver<any>) ***REMOVED***
  ***REMOVED***
***REMOVED***

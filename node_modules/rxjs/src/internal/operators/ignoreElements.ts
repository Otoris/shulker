import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** Operator ***REMOVED*** from '../Operator';
import ***REMOVED*** Subscriber ***REMOVED*** from '../Subscriber';
import ***REMOVED*** OperatorFunction ***REMOVED*** from '../types';

/**
 * Ignores all items emitted by the source Observable and only passes calls of `complete` or `error`.
 *
 * ![](ignoreElements.png)
 *
 * ## Examples
 * ### Ignores emitted values, reacts to observable's completion.
 * ```ts
 * import ***REMOVED*** of ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** ignoreElements ***REMOVED*** from 'rxjs/operators';
 *
 * of('you', 'talking', 'to', 'me').pipe(
 *   ignoreElements(),
 * )
 * .subscribe(
 *   word => console.log(word),
 *   err => console.log('error:', err),
 *   () => console.log('the end'),
 * );
 * // result:
 * // 'the end'
 * ```
 * @return ***REMOVED***Observable***REMOVED*** An empty Observable that only calls `complete`
 * or `error`, based on which one is called by the source Observable.
 * @method ignoreElements
 * @owner Observable
 */
export function ignoreElements(): OperatorFunction<any, never> ***REMOVED***
  return function ignoreElementsOperatorFunction(source: Observable<any>) ***REMOVED***
    return source.lift(new IgnoreElementsOperator());
  ***REMOVED***;
***REMOVED***

class IgnoreElementsOperator<T, R> implements Operator<T, R> ***REMOVED***
  call(subscriber: Subscriber<R>, source: any): any ***REMOVED***
    return source.subscribe(new IgnoreElementsSubscriber(subscriber));
  ***REMOVED***
***REMOVED***

/**
 * We need this JSDoc comment for affecting ESDoc.
 * @ignore
 * @extends ***REMOVED***Ignored***REMOVED***
 */
class IgnoreElementsSubscriber<T> extends Subscriber<T> ***REMOVED***
  protected _next(unused: T): void ***REMOVED***
    // Do nothing
  ***REMOVED***
***REMOVED***

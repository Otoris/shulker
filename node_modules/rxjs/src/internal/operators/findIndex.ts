import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** FindValueOperator ***REMOVED*** from '../operators/find';
import ***REMOVED*** OperatorFunction ***REMOVED*** from '../types';
/**
 * Emits only the index of the first value emitted by the source Observable that
 * meets some condition.
 *
 * <span class="informal">It's like ***REMOVED***@link find***REMOVED***, but emits the index of the
 * found value, not the value itself.</span>
 *
 * ![](findIndex.png)
 *
 * `findIndex` searches for the first item in the source Observable that matches
 * the specified condition embodied by the `predicate`, and returns the
 * (zero-based) index of the first occurrence in the source. Unlike
 * ***REMOVED***@link first***REMOVED***, the `predicate` is required in `findIndex`, and does not emit
 * an error if a valid value is not found.
 *
 * ## Example
 * Emit the index of first click that happens on a DIV element
 * ```ts
 * import ***REMOVED*** fromEvent ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** findIndex ***REMOVED*** from 'rxjs/operators';
 *
 * const clicks = fromEvent(document, 'click');
 * const result = clicks.pipe(findIndex(ev => ev.target.tagName === 'DIV'));
 * result.subscribe(x => console.log(x));
 * ```
 *
 * @see ***REMOVED***@link filter***REMOVED***
 * @see ***REMOVED***@link find***REMOVED***
 * @see ***REMOVED***@link first***REMOVED***
 * @see ***REMOVED***@link take***REMOVED***
 *
 * @param ***REMOVED***function(value: T, index: number, source: Observable<T>): boolean***REMOVED*** predicate
 * A function called with each item to test for condition matching.
 * @param ***REMOVED***any***REMOVED*** [thisArg] An optional argument to determine the value of `this`
 * in the `predicate` function.
 * @return ***REMOVED***Observable***REMOVED*** An Observable of the index of the first item that
 * matches the condition.
 * @method find
 * @owner Observable
 */
export function findIndex<T>(predicate: (value: T, index: number, source: Observable<T>) => boolean,
                             thisArg?: any): OperatorFunction<T, number> ***REMOVED***
  return (source: Observable<T>) => source.lift(new FindValueOperator(predicate, source, true, thisArg)) as Observable<any>;
***REMOVED***

import ***REMOVED*** Operator ***REMOVED*** from '../Operator';
import ***REMOVED*** Subscriber ***REMOVED*** from '../Subscriber';
import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** Subscription ***REMOVED*** from '../Subscription';
import ***REMOVED*** MonoTypeOperatorFunction, SubscribableOrPromise, TeardownLogic ***REMOVED*** from '../types';

import ***REMOVED*** OuterSubscriber ***REMOVED*** from '../OuterSubscriber';
import ***REMOVED*** subscribeToResult ***REMOVED*** from '../util/subscribeToResult';

/**
 * Ignores source values for a duration determined by another Observable, then
 * emits the most recent value from the source Observable, then repeats this
 * process.
 *
 * <span class="informal">It's like ***REMOVED***@link auditTime***REMOVED***, but the silencing
 * duration is determined by a second Observable.</span>
 *
 * ![](audit.png)
 *
 * `audit` is similar to `throttle`, but emits the last value from the silenced
 * time window, instead of the first value. `audit` emits the most recent value
 * from the source Observable on the output Observable as soon as its internal
 * timer becomes disabled, and ignores source values while the timer is enabled.
 * Initially, the timer is disabled. As soon as the first source value arrives,
 * the timer is enabled by calling the `durationSelector` function with the
 * source value, which returns the "duration" Observable. When the duration
 * Observable emits a value or completes, the timer is disabled, then the most
 * recent source value is emitted on the output Observable, and this process
 * repeats for the next source value.
 *
 * ## Example
 *
 * Emit clicks at a rate of at most one click per second
 * ```ts
 * import ***REMOVED*** fromEvent, interval ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** audit ***REMOVED*** from 'rxjs/operators'
 *
 * const clicks = fromEvent(document, 'click');
 * const result = clicks.pipe(audit(ev => interval(1000)));
 * result.subscribe(x => console.log(x));
 * ```
 * @see ***REMOVED***@link auditTime***REMOVED***
 * @see ***REMOVED***@link debounce***REMOVED***
 * @see ***REMOVED***@link delayWhen***REMOVED***
 * @see ***REMOVED***@link sample***REMOVED***
 * @see ***REMOVED***@link throttle***REMOVED***
 *
 * @param ***REMOVED***function(value: T): SubscribableOrPromise***REMOVED*** durationSelector A function
 * that receives a value from the source Observable, for computing the silencing
 * duration, returned as an Observable or a Promise.
 * @return ***REMOVED***Observable<T>***REMOVED*** An Observable that performs rate-limiting of
 * emissions from the source Observable.
 * @method audit
 * @owner Observable
 */
export function audit<T>(durationSelector: (value: T) => SubscribableOrPromise<any>): MonoTypeOperatorFunction<T> ***REMOVED***
  return function auditOperatorFunction(source: Observable<T>) ***REMOVED***
    return source.lift(new AuditOperator(durationSelector));
  ***REMOVED***;
***REMOVED***

class AuditOperator<T> implements Operator<T, T> ***REMOVED***
  constructor(private durationSelector: (value: T) => SubscribableOrPromise<any>) ***REMOVED***
  ***REMOVED***

  call(subscriber: Subscriber<T>, source: any): TeardownLogic ***REMOVED***
    return source.subscribe(new AuditSubscriber<T, T>(subscriber, this.durationSelector));
  ***REMOVED***
***REMOVED***

/**
 * We need this JSDoc comment for affecting ESDoc.
 * @ignore
 * @extends ***REMOVED***Ignored***REMOVED***
 */
class AuditSubscriber<T, R> extends OuterSubscriber<T, R> ***REMOVED***

  private value: T;
  private hasValue: boolean = false;
  private throttled: Subscription;

  constructor(destination: Subscriber<T>,
              private durationSelector: (value: T) => SubscribableOrPromise<any>) ***REMOVED***
    super(destination);
  ***REMOVED***

  protected _next(value: T): void ***REMOVED***
    this.value = value;
    this.hasValue = true;
    if (!this.throttled) ***REMOVED***
      let duration;
      try ***REMOVED***
        const ***REMOVED*** durationSelector ***REMOVED*** = this;
        duration = durationSelector(value);
      ***REMOVED*** catch (err) ***REMOVED***
        return this.destination.error(err);
      ***REMOVED***
      const innerSubscription = subscribeToResult(this, duration);
      if (!innerSubscription || innerSubscription.closed) ***REMOVED***
        this.clearThrottle();
      ***REMOVED*** else ***REMOVED***
        this.add(this.throttled = innerSubscription);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  clearThrottle() ***REMOVED***
    const ***REMOVED*** value, hasValue, throttled ***REMOVED*** = this;
    if (throttled) ***REMOVED***
      this.remove(throttled);
      this.throttled = null;
      throttled.unsubscribe();
    ***REMOVED***
    if (hasValue) ***REMOVED***
      this.value = null;
      this.hasValue = false;
      this.destination.next(value);
    ***REMOVED***
  ***REMOVED***

  notifyNext(outerValue: T, innerValue: R, outerIndex: number, innerIndex: number): void ***REMOVED***
    this.clearThrottle();
  ***REMOVED***

  notifyComplete(): void ***REMOVED***
    this.clearThrottle();
  ***REMOVED***
***REMOVED***

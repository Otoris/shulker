import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** Operator ***REMOVED*** from '../Operator';
import ***REMOVED*** Observer, OperatorFunction ***REMOVED*** from '../types';
import ***REMOVED*** Subscriber ***REMOVED*** from '../Subscriber';
/**
 * Counts the number of emissions on the source and emits that number when the
 * source completes.
 *
 * <span class="informal">Tells how many values were emitted, when the source
 * completes.</span>
 *
 * ![](count.png)
 *
 * `count` transforms an Observable that emits values into an Observable that
 * emits a single value that represents the number of values emitted by the
 * source Observable. If the source Observable terminates with an error, `count`
 * will pass this error notification along without emitting a value first. If
 * the source Observable does not terminate at all, `count` will neither emit
 * a value nor terminate. This operator takes an optional `predicate` function
 * as argument, in which case the output emission will represent the number of
 * source values that matched `true` with the `predicate`.
 *
 * ## Examples
 *
 * Counts how many seconds have passed before the first click happened
 * ```ts
 * import ***REMOVED*** fromEvent, interval ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** count, takeUntil ***REMOVED*** from 'rxjs/operators';
 *
 * const seconds = interval(1000);
 * const clicks = fromEvent(document, 'click');
 * const secondsBeforeClick = seconds.pipe(takeUntil(clicks));
 * const result = secondsBeforeClick.pipe(count());
 * result.subscribe(x => console.log(x));
 * ```
 *
 * Counts how many odd numbers are there between 1 and 7
 * ```ts
 * import ***REMOVED*** range ***REMOVED*** from 'rxjs';
 * import ***REMOVED*** count ***REMOVED*** from 'rxjs/operators';
 *
 * const numbers = range(1, 7);
 * const result = numbers.pipe(count(i => i % 2 === 1));
 * result.subscribe(x => console.log(x));
 * // Results in:
 * // 4
 * ```
 *
 * @see ***REMOVED***@link max***REMOVED***
 * @see ***REMOVED***@link min***REMOVED***
 * @see ***REMOVED***@link reduce***REMOVED***
 *
 * @param ***REMOVED***function(value: T, i: number, source: Observable<T>): boolean***REMOVED*** [predicate] A
 * boolean function to select what values are to be counted. It is provided with
 * arguments of:
 * - `value`: the value from the source Observable.
 * - `index`: the (zero-based) "index" of the value from the source Observable.
 * - `source`: the source Observable instance itself.
 * @return ***REMOVED***Observable***REMOVED*** An Observable of one number that represents the count as
 * described above.
 * @method count
 * @owner Observable
 */

export function count<T>(predicate?: (value: T, index: number, source: Observable<T>) => boolean): OperatorFunction<T, number> ***REMOVED***
  return (source: Observable<T>) => source.lift(new CountOperator(predicate, source));
***REMOVED***

class CountOperator<T> implements Operator<T, number> ***REMOVED***
  constructor(private predicate?: (value: T, index: number, source: Observable<T>) => boolean,
              private source?: Observable<T>) ***REMOVED***
  ***REMOVED***

  call(subscriber: Subscriber<number>, source: any): any ***REMOVED***
    return source.subscribe(new CountSubscriber(subscriber, this.predicate, this.source));
  ***REMOVED***
***REMOVED***

/**
 * We need this JSDoc comment for affecting ESDoc.
 * @ignore
 * @extends ***REMOVED***Ignored***REMOVED***
 */
class CountSubscriber<T> extends Subscriber<T> ***REMOVED***
  private count: number = 0;
  private index: number = 0;

  constructor(destination: Observer<number>,
              private predicate?: (value: T, index: number, source: Observable<T>) => boolean,
              private source?: Observable<T>) ***REMOVED***
    super(destination);
  ***REMOVED***

  protected _next(value: T): void ***REMOVED***
    if (this.predicate) ***REMOVED***
      this._tryPredicate(value);
    ***REMOVED*** else ***REMOVED***
      this.count++;
    ***REMOVED***
  ***REMOVED***

  private _tryPredicate(value: T) ***REMOVED***
    let result: any;

    try ***REMOVED***
      result = this.predicate(value, this.index++, this.source);
    ***REMOVED*** catch (err) ***REMOVED***
      this.destination.error(err);
      return;
    ***REMOVED***

    if (result) ***REMOVED***
      this.count++;
    ***REMOVED***
  ***REMOVED***

  protected _complete(): void ***REMOVED***
    this.destination.next(this.count);
    this.destination.complete();
  ***REMOVED***
***REMOVED***

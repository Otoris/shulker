import ***REMOVED*** Observable ***REMOVED*** from '../Observable';
import ***REMOVED*** SchedulerLike ***REMOVED*** from '../types';
import ***REMOVED*** Subscription ***REMOVED*** from '../Subscription';

export function scheduleArray<T>(input: ArrayLike<T>, scheduler: SchedulerLike) ***REMOVED***
  return new Observable<T>(subscriber => ***REMOVED***
    const sub = new Subscription();
    let i = 0;
    sub.add(scheduler.schedule(function () ***REMOVED***
      if (i === input.length) ***REMOVED***
        subscriber.complete();
        return;
      ***REMOVED***
      subscriber.next(input[i++]);
      if (!subscriber.closed) ***REMOVED***
        sub.add(this.schedule());
      ***REMOVED***
    ***REMOVED***));
    return sub;
  ***REMOVED***);
***REMOVED***

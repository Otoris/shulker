import ***REMOVED*** Subscriber ***REMOVED*** from '../Subscriber';
import ***REMOVED*** Subject ***REMOVED*** from '../Subject';

/**
 * Determines whether the ErrorObserver is closed or stopped or has a
 * destination that is closed or stopped - in which case errors will
 * need to be reported via a different mechanism.
 * @param observer the observer
 */
export function canReportError(observer: Subscriber<any> | Subject<any>): boolean ***REMOVED***
  while (observer) ***REMOVED***
    const ***REMOVED*** closed, destination, isStopped ***REMOVED*** = observer as any;
    if (closed || isStopped) ***REMOVED***
      return false;
    ***REMOVED*** else if (destination && destination instanceof Subscriber) ***REMOVED***
      observer = destination;
    ***REMOVED*** else ***REMOVED***
      observer = null;
    ***REMOVED***
  ***REMOVED***
  return true;
***REMOVED***

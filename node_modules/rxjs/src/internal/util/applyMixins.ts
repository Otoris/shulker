export function applyMixins(derivedCtor: any, baseCtors: any[]) ***REMOVED***
  for (let i = 0, len = baseCtors.length; i < len; i++) ***REMOVED***
    const baseCtor = baseCtors[i];
    const propertyKeys = Object.getOwnPropertyNames(baseCtor.prototype);
    for (let j = 0, len2 = propertyKeys.length; j < len2; j++) ***REMOVED***
      const name = propertyKeys[j];
      derivedCtor.prototype[name] = baseCtor.prototype[name];
    ***REMOVED***
  ***REMOVED***
***REMOVED***
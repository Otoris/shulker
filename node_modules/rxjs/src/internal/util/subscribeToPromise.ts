import ***REMOVED*** Subscriber ***REMOVED*** from '../Subscriber';
import ***REMOVED*** hostReportError ***REMOVED*** from './hostReportError';

export const subscribeToPromise = <T>(promise: PromiseLike<T>) => (subscriber: Subscriber<T>) => ***REMOVED***
  promise.then(
    (value) => ***REMOVED***
      if (!subscriber.closed) ***REMOVED***
        subscriber.next(value);
        subscriber.complete();
      ***REMOVED***
    ***REMOVED***,
    (err: any) => subscriber.error(err)
  )
  .then(null, hostReportError);
  return subscriber;
***REMOVED***;

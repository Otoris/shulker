import ***REMOVED*** Immediate ***REMOVED*** from '../util/Immediate';
import ***REMOVED*** AsyncAction ***REMOVED*** from './AsyncAction';
import ***REMOVED*** AsapScheduler ***REMOVED*** from './AsapScheduler';
import ***REMOVED*** SchedulerAction ***REMOVED*** from '../types';
/**
 * We need this JSDoc comment for affecting ESDoc.
 * @ignore
 * @extends ***REMOVED***Ignored***REMOVED***
 */
export class AsapAction<T> extends AsyncAction<T> ***REMOVED***

  constructor(protected scheduler: AsapScheduler,
              protected work: (this: SchedulerAction<T>, state?: T) => void) ***REMOVED***
    super(scheduler, work);
  ***REMOVED***

  protected requestAsyncId(scheduler: AsapScheduler, id?: any, delay: number = 0): any ***REMOVED***
    // If delay is greater than 0, request as an async action.
    if (delay !== null && delay > 0) ***REMOVED***
      return super.requestAsyncId(scheduler, id, delay);
    ***REMOVED***
    // Push the action to the end of the scheduler queue.
    scheduler.actions.push(this);
    // If a microtask has already been scheduled, don't schedule another
    // one. If a microtask hasn't been scheduled yet, schedule one now. Return
    // the current scheduled microtask id.
    return scheduler.scheduled || (scheduler.scheduled = Immediate.setImmediate(
      scheduler.flush.bind(scheduler, null)
    ));
  ***REMOVED***
  protected recycleAsyncId(scheduler: AsapScheduler, id?: any, delay: number = 0): any ***REMOVED***
    // If delay exists and is greater than 0, or if the delay is null (the
    // action wasn't rescheduled) but was originally scheduled as an async
    // action, then recycle as an async action.
    if ((delay !== null && delay > 0) || (delay === null && this.delay > 0)) ***REMOVED***
      return super.recycleAsyncId(scheduler, id, delay);
    ***REMOVED***
    // If the scheduler queue is empty, cancel the requested microtask and
    // set the scheduled flag to undefined so the next AsapAction will schedule
    // its own.
    if (scheduler.actions.length === 0) ***REMOVED***
      Immediate.clearImmediate(id);
      scheduler.scheduled = undefined;
    ***REMOVED***
    // Return undefined so the action knows to request a new async id if it's rescheduled.
    return undefined;
  ***REMOVED***
***REMOVED***

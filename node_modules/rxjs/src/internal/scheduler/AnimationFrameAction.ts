import ***REMOVED*** AsyncAction ***REMOVED*** from './AsyncAction';
import ***REMOVED*** AnimationFrameScheduler ***REMOVED*** from './AnimationFrameScheduler';
import ***REMOVED*** SchedulerAction ***REMOVED*** from '../types';

/**
 * We need this JSDoc comment for affecting ESDoc.
 * @ignore
 * @extends ***REMOVED***Ignored***REMOVED***
 */
export class AnimationFrameAction<T> extends AsyncAction<T> ***REMOVED***

  constructor(protected scheduler: AnimationFrameScheduler,
              protected work: (this: SchedulerAction<T>, state?: T) => void) ***REMOVED***
    super(scheduler, work);
  ***REMOVED***

  protected requestAsyncId(scheduler: AnimationFrameScheduler, id?: any, delay: number = 0): any ***REMOVED***
    // If delay is greater than 0, request as an async action.
    if (delay !== null && delay > 0) ***REMOVED***
      return super.requestAsyncId(scheduler, id, delay);
    ***REMOVED***
    // Push the action to the end of the scheduler queue.
    scheduler.actions.push(this);
    // If an animation frame has already been requested, don't request another
    // one. If an animation frame hasn't been requested yet, request one. Return
    // the current animation frame request id.
    return scheduler.scheduled || (scheduler.scheduled = requestAnimationFrame(
      () => scheduler.flush(null)));
  ***REMOVED***
  protected recycleAsyncId(scheduler: AnimationFrameScheduler, id?: any, delay: number = 0): any ***REMOVED***
    // If delay exists and is greater than 0, or if the delay is null (the
    // action wasn't rescheduled) but was originally scheduled as an async
    // action, then recycle as an async action.
    if ((delay !== null && delay > 0) || (delay === null && this.delay > 0)) ***REMOVED***
      return super.recycleAsyncId(scheduler, id, delay);
    ***REMOVED***
    // If the scheduler queue is empty, cancel the requested animation frame and
    // set the scheduled flag to undefined so the next AnimationFrameAction will
    // request its own.
    if (scheduler.actions.length === 0) ***REMOVED***
      cancelAnimationFrame(id);
      scheduler.scheduled = undefined;
    ***REMOVED***
    // Return undefined so the action knows to request a new async id if it's rescheduled.
    return undefined;
  ***REMOVED***
***REMOVED***

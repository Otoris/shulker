let _enable_super_gross_mode_that_will_cause_bad_things = false;

/**
 * The global configuration object for RxJS, used to configure things
 * like what Promise contructor should used to create Promises
 */
export const config = ***REMOVED***
  /**
   * The promise constructor used by default for methods such as
   * ***REMOVED***@link toPromise***REMOVED*** and ***REMOVED***@link forEach***REMOVED***
   */
  Promise: undefined as PromiseConstructorLike,

  /**
   * If true, turns on synchronous error rethrowing, which is a deprecated behavior
   * in v6 and higher. This behavior enables bad patterns like wrapping a subscribe
   * call in a try/catch block. It also enables producer interference, a nasty bug
   * where a multicast can be broken for all observers by a downstream consumer with
   * an unhandled error. DO NOT USE THIS FLAG UNLESS IT'S NEEDED TO BY TIME
   * FOR MIGRATION REASONS.
   */
  set useDeprecatedSynchronousErrorHandling(value: boolean) ***REMOVED***
    if (value) ***REMOVED***
      const error = new Error();
      console.warn('DEPRECATED! RxJS was set to use deprecated synchronous error handling behavior by code at: \n' + error.stack);
    ***REMOVED*** else if (_enable_super_gross_mode_that_will_cause_bad_things) ***REMOVED***
      console.log('RxJS: Back to a better error behavior. Thank you. <3');
    ***REMOVED***
    _enable_super_gross_mode_that_will_cause_bad_things = value;
  ***REMOVED***,

  get useDeprecatedSynchronousErrorHandling() ***REMOVED***
    return _enable_super_gross_mode_that_will_cause_bad_things;
  ***REMOVED***,
***REMOVED***;

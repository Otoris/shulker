import ***REMOVED*** map ***REMOVED*** from './map';
export function pluck(...properties) ***REMOVED***
    const length = properties.length;
    if (length === 0) ***REMOVED***
        throw new Error('list of properties cannot be empty.');
    ***REMOVED***
    return (source) => map(plucker(properties, length))(source);
***REMOVED***
function plucker(props, length) ***REMOVED***
    const mapper = (x) => ***REMOVED***
        let currentProp = x;
        for (let i = 0; i < length; i++) ***REMOVED***
            const p = currentProp[props[i]];
            if (typeof p !== 'undefined') ***REMOVED***
                currentProp = p;
            ***REMOVED***
            else ***REMOVED***
                return undefined;
            ***REMOVED***
        ***REMOVED***
        return currentProp;
    ***REMOVED***;
    return mapper;
***REMOVED***
//# sourceMappingURL=pluck.js.map
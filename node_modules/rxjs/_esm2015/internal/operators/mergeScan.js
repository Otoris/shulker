import ***REMOVED*** subscribeToResult ***REMOVED*** from '../util/subscribeToResult';
import ***REMOVED*** OuterSubscriber ***REMOVED*** from '../OuterSubscriber';
import ***REMOVED*** InnerSubscriber ***REMOVED*** from '../InnerSubscriber';
export function mergeScan(accumulator, seed, concurrent = Number.POSITIVE_INFINITY) ***REMOVED***
    return (source) => source.lift(new MergeScanOperator(accumulator, seed, concurrent));
***REMOVED***
export class MergeScanOperator ***REMOVED***
    constructor(accumulator, seed, concurrent) ***REMOVED***
        this.accumulator = accumulator;
        this.seed = seed;
        this.concurrent = concurrent;
    ***REMOVED***
    call(subscriber, source) ***REMOVED***
        return source.subscribe(new MergeScanSubscriber(subscriber, this.accumulator, this.seed, this.concurrent));
    ***REMOVED***
***REMOVED***
export class MergeScanSubscriber extends OuterSubscriber ***REMOVED***
    constructor(destination, accumulator, acc, concurrent) ***REMOVED***
        super(destination);
        this.accumulator = accumulator;
        this.acc = acc;
        this.concurrent = concurrent;
        this.hasValue = false;
        this.hasCompleted = false;
        this.buffer = [];
        this.active = 0;
        this.index = 0;
    ***REMOVED***
    _next(value) ***REMOVED***
        if (this.active < this.concurrent) ***REMOVED***
            const index = this.index++;
            const destination = this.destination;
            let ish;
            try ***REMOVED***
                const ***REMOVED*** accumulator ***REMOVED*** = this;
                ish = accumulator(this.acc, value, index);
            ***REMOVED***
            catch (e) ***REMOVED***
                return destination.error(e);
            ***REMOVED***
            this.active++;
            this._innerSub(ish, value, index);
        ***REMOVED***
        else ***REMOVED***
            this.buffer.push(value);
        ***REMOVED***
    ***REMOVED***
    _innerSub(ish, value, index) ***REMOVED***
        const innerSubscriber = new InnerSubscriber(this, value, index);
        const destination = this.destination;
        destination.add(innerSubscriber);
        const innerSubscription = subscribeToResult(this, ish, undefined, undefined, innerSubscriber);
        if (innerSubscription !== innerSubscriber) ***REMOVED***
            destination.add(innerSubscription);
        ***REMOVED***
    ***REMOVED***
    _complete() ***REMOVED***
        this.hasCompleted = true;
        if (this.active === 0 && this.buffer.length === 0) ***REMOVED***
            if (this.hasValue === false) ***REMOVED***
                this.destination.next(this.acc);
            ***REMOVED***
            this.destination.complete();
        ***REMOVED***
        this.unsubscribe();
    ***REMOVED***
    notifyNext(outerValue, innerValue, outerIndex, innerIndex, innerSub) ***REMOVED***
        const ***REMOVED*** destination ***REMOVED*** = this;
        this.acc = innerValue;
        this.hasValue = true;
        destination.next(innerValue);
    ***REMOVED***
    notifyComplete(innerSub) ***REMOVED***
        const buffer = this.buffer;
        const destination = this.destination;
        destination.remove(innerSub);
        this.active--;
        if (buffer.length > 0) ***REMOVED***
            this._next(buffer.shift());
        ***REMOVED***
        else if (this.active === 0 && this.hasCompleted) ***REMOVED***
            if (this.hasValue === false) ***REMOVED***
                this.destination.next(this.acc);
            ***REMOVED***
            this.destination.complete();
        ***REMOVED***
    ***REMOVED***
***REMOVED***
//# sourceMappingURL=mergeScan.js.map
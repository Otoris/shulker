import ***REMOVED*** ReplaySubject ***REMOVED*** from '../ReplaySubject';
import ***REMOVED*** multicast ***REMOVED*** from './multicast';
export function publishReplay(bufferSize, windowTime, selectorOrScheduler, scheduler) ***REMOVED***
    if (selectorOrScheduler && typeof selectorOrScheduler !== 'function') ***REMOVED***
        scheduler = selectorOrScheduler;
    ***REMOVED***
    const selector = typeof selectorOrScheduler === 'function' ? selectorOrScheduler : undefined;
    const subject = new ReplaySubject(bufferSize, windowTime, scheduler);
    return (source) => multicast(() => subject, selector)(source);
***REMOVED***
//# sourceMappingURL=publishReplay.js.map
import ***REMOVED*** reduce ***REMOVED*** from './reduce';
export function max(comparer) ***REMOVED***
    const max = (typeof comparer === 'function')
        ? (x, y) => comparer(x, y) > 0 ? x : y
        : (x, y) => x > y ? x : y;
    return reduce(max);
***REMOVED***
//# sourceMappingURL=max.js.map
import ***REMOVED*** reduce ***REMOVED*** from './reduce';
export function min(comparer) ***REMOVED***
    const min = (typeof comparer === 'function')
        ? (x, y) => comparer(x, y) < 0 ? x : y
        : (x, y) => x < y ? x : y;
    return reduce(min);
***REMOVED***
//# sourceMappingURL=min.js.map
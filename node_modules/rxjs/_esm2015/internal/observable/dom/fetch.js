import ***REMOVED*** Observable ***REMOVED*** from '../../Observable';
export function fromFetch(input, init) ***REMOVED***
    return new Observable(subscriber => ***REMOVED***
        const controller = new AbortController();
        const signal = controller.signal;
        let outerSignalHandler;
        let abortable = true;
        let unsubscribed = false;
        if (init) ***REMOVED***
            if (init.signal) ***REMOVED***
                if (init.signal.aborted) ***REMOVED***
                    controller.abort();
                ***REMOVED***
                else ***REMOVED***
                    outerSignalHandler = () => ***REMOVED***
                        if (!signal.aborted) ***REMOVED***
                            controller.abort();
                        ***REMOVED***
                    ***REMOVED***;
                    init.signal.addEventListener('abort', outerSignalHandler);
                ***REMOVED***
            ***REMOVED***
            init = Object.assign(***REMOVED******REMOVED***, init, ***REMOVED*** signal ***REMOVED***);
        ***REMOVED***
        else ***REMOVED***
            init = ***REMOVED*** signal ***REMOVED***;
        ***REMOVED***
        fetch(input, init).then(response => ***REMOVED***
            abortable = false;
            subscriber.next(response);
            subscriber.complete();
        ***REMOVED***).catch(err => ***REMOVED***
            abortable = false;
            if (!unsubscribed) ***REMOVED***
                subscriber.error(err);
            ***REMOVED***
        ***REMOVED***);
        return () => ***REMOVED***
            unsubscribed = true;
            if (abortable) ***REMOVED***
                controller.abort();
            ***REMOVED***
        ***REMOVED***;
    ***REMOVED***);
***REMOVED***
//# sourceMappingURL=fetch.js.map
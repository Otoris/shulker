import ***REMOVED*** Subscription ***REMOVED*** from './Subscription';
export class SubjectSubscription extends Subscription ***REMOVED***
    constructor(subject, subscriber) ***REMOVED***
        super();
        this.subject = subject;
        this.subscriber = subscriber;
        this.closed = false;
    ***REMOVED***
    unsubscribe() ***REMOVED***
        if (this.closed) ***REMOVED***
            return;
        ***REMOVED***
        this.closed = true;
        const subject = this.subject;
        const observers = subject.observers;
        this.subject = null;
        if (!observers || observers.length === 0 || subject.isStopped || subject.closed) ***REMOVED***
            return;
        ***REMOVED***
        const subscriberIndex = observers.indexOf(this.subscriber);
        if (subscriberIndex !== -1) ***REMOVED***
            observers.splice(subscriberIndex, 1);
        ***REMOVED***
    ***REMOVED***
***REMOVED***
//# sourceMappingURL=SubjectSubscription.js.map
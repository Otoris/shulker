export const subscribeToArray = (array) => (subscriber) => ***REMOVED***
    for (let i = 0, len = array.length; i < len && !subscriber.closed; i++) ***REMOVED***
        subscriber.next(array[i]);
    ***REMOVED***
    subscriber.complete();
***REMOVED***;
//# sourceMappingURL=subscribeToArray.js.map
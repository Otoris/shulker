'use strict';

var path = require('path');

function containsPath(fp, segment) ***REMOVED***
  if (typeof fp !== 'string' || typeof segment !== 'string') ***REMOVED***
    throw new TypeError('contains-path expects file paths to be a string.');
  ***REMOVED***

  var prefix = '(^|\\/)';
  if (segment.indexOf('./') === 0 || segment.charAt(0) === '/') ***REMOVED***
    prefix = '^';
  ***REMOVED***

  var re = new RegExp(prefix + normalize(segment).join('\\/') + '($|\\/)');
  fp = normalize(fp).join('/');
  return re.test(fp);
***REMOVED***

/**
 * Normalize slashes
 */

function normalize(str) ***REMOVED***
  str = path.normalize(str);
  return str.split(/[\\\/]+/);
***REMOVED***

/**
 * Expose `containsPath`
 */

module.exports = containsPath;

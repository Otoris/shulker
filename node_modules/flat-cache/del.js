var rimraf = require( 'rimraf' ).sync;
var fs = require( 'fs' );

module.exports = function del( file ) ***REMOVED***
  if ( fs.existsSync( file ) ) ***REMOVED***
    //if rimraf doesn't throw then the file has been deleted or didn't exist
    rimraf( file, ***REMOVED***
      glob: false
    ***REMOVED*** );
    return true;
  ***REMOVED***
  return false;
***REMOVED***;

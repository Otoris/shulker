/**
 * @fileoverview Define the cursor which limits the number of tokens.
 * @author Toru Nagashima
 */
"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

const DecorativeCursor = require("./decorative-cursor");

//------------------------------------------------------------------------------
// Exports
//------------------------------------------------------------------------------

/**
 * The decorative cursor which limits the number of tokens.
 */
module.exports = class LimitCursor extends DecorativeCursor ***REMOVED***

    /**
     * Initializes this cursor.
     * @param ***REMOVED***Cursor***REMOVED*** cursor - The cursor to be decorated.
     * @param ***REMOVED***number***REMOVED*** count - The count of tokens this cursor iterates.
     */
    constructor(cursor, count) ***REMOVED***
        super(cursor);
        this.count = count;
    ***REMOVED***

    /** @inheritdoc */
    moveNext() ***REMOVED***
        if (this.count > 0) ***REMOVED***
            this.count -= 1;
            return super.moveNext();
        ***REMOVED***
        return false;
    ***REMOVED***
***REMOVED***;

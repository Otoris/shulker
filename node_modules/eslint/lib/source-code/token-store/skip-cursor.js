/**
 * @fileoverview Define the cursor which ignores the first few tokens.
 * @author Toru Nagashima
 */
"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

const DecorativeCursor = require("./decorative-cursor");

//------------------------------------------------------------------------------
// Exports
//------------------------------------------------------------------------------

/**
 * The decorative cursor which ignores the first few tokens.
 */
module.exports = class SkipCursor extends DecorativeCursor ***REMOVED***

    /**
     * Initializes this cursor.
     * @param ***REMOVED***Cursor***REMOVED*** cursor - The cursor to be decorated.
     * @param ***REMOVED***number***REMOVED*** count - The count of tokens this cursor skips.
     */
    constructor(cursor, count) ***REMOVED***
        super(cursor);
        this.count = count;
    ***REMOVED***

    /** @inheritdoc */
    moveNext() ***REMOVED***
        while (this.count > 0) ***REMOVED***
            this.count -= 1;
            if (!super.moveNext()) ***REMOVED***
                return false;
            ***REMOVED***
        ***REMOVED***
        return super.moveNext();
    ***REMOVED***
***REMOVED***;

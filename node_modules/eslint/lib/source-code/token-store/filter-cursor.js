/**
 * @fileoverview Define the cursor which ignores specified tokens.
 * @author Toru Nagashima
 */
"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

const DecorativeCursor = require("./decorative-cursor");

//------------------------------------------------------------------------------
// Exports
//------------------------------------------------------------------------------

/**
 * The decorative cursor which ignores specified tokens.
 */
module.exports = class FilterCursor extends DecorativeCursor ***REMOVED***

    /**
     * Initializes this cursor.
     * @param ***REMOVED***Cursor***REMOVED*** cursor - The cursor to be decorated.
     * @param ***REMOVED***Function***REMOVED*** predicate - The predicate function to decide tokens this cursor iterates.
     */
    constructor(cursor, predicate) ***REMOVED***
        super(cursor);
        this.predicate = predicate;
    ***REMOVED***

    /** @inheritdoc */
    moveNext() ***REMOVED***
        const predicate = this.predicate;

        while (super.moveNext()) ***REMOVED***
            if (predicate(this.current)) ***REMOVED***
                return true;
            ***REMOVED***
        ***REMOVED***
        return false;
    ***REMOVED***
***REMOVED***;

/**
 * @fileoverview Rule to require newlines before `return` statement
 * @author Kai Cataldo
 * @deprecated
 */
"use strict";

//------------------------------------------------------------------------------
// Rule Definition
//------------------------------------------------------------------------------

module.exports = ***REMOVED***
    meta: ***REMOVED***
        type: "layout",

        docs: ***REMOVED***
            description: "require an empty line before `return` statements",
            category: "Stylistic Issues",
            recommended: false,
            url: "https://eslint.org/docs/rules/newline-before-return"
        ***REMOVED***,

        fixable: "whitespace",
        schema: [],
        messages: ***REMOVED***
            expected: "Expected newline before return statement."
        ***REMOVED***,

        deprecated: true,
        replacedBy: ["padding-line-between-statements"]
    ***REMOVED***,

    create(context) ***REMOVED***
        const sourceCode = context.getSourceCode();

        //--------------------------------------------------------------------------
        // Helpers
        //--------------------------------------------------------------------------

        /**
         * Tests whether node is preceded by supplied tokens
         * @param ***REMOVED***ASTNode***REMOVED*** node - node to check
         * @param ***REMOVED***Array***REMOVED*** testTokens - array of tokens to test against
         * @returns ***REMOVED***boolean***REMOVED*** Whether or not the node is preceded by one of the supplied tokens
         * @private
         */
        function isPrecededByTokens(node, testTokens) ***REMOVED***
            const tokenBefore = sourceCode.getTokenBefore(node);

            return testTokens.some(token => tokenBefore.value === token);
        ***REMOVED***

        /**
         * Checks whether node is the first node after statement or in block
         * @param ***REMOVED***ASTNode***REMOVED*** node - node to check
         * @returns ***REMOVED***boolean***REMOVED*** Whether or not the node is the first node after statement or in block
         * @private
         */
        function isFirstNode(node) ***REMOVED***
            const parentType = node.parent.type;

            if (node.parent.body) ***REMOVED***
                return Array.isArray(node.parent.body)
                    ? node.parent.body[0] === node
                    : node.parent.body === node;
            ***REMOVED***

            if (parentType === "IfStatement") ***REMOVED***
                return isPrecededByTokens(node, ["else", ")"]);
            ***REMOVED***
            if (parentType === "DoWhileStatement") ***REMOVED***
                return isPrecededByTokens(node, ["do"]);
            ***REMOVED***
            if (parentType === "SwitchCase") ***REMOVED***
                return isPrecededByTokens(node, [":"]);
            ***REMOVED***
            return isPrecededByTokens(node, [")"]);

        ***REMOVED***

        /**
         * Returns the number of lines of comments that precede the node
         * @param ***REMOVED***ASTNode***REMOVED*** node - node to check for overlapping comments
         * @param ***REMOVED***number***REMOVED*** lineNumTokenBefore - line number of previous token, to check for overlapping comments
         * @returns ***REMOVED***number***REMOVED*** Number of lines of comments that precede the node
         * @private
         */
        function calcCommentLines(node, lineNumTokenBefore) ***REMOVED***
            const comments = sourceCode.getCommentsBefore(node);
            let numLinesComments = 0;

            if (!comments.length) ***REMOVED***
                return numLinesComments;
            ***REMOVED***

            comments.forEach(comment => ***REMOVED***
                numLinesComments++;

                if (comment.type === "Block") ***REMOVED***
                    numLinesComments += comment.loc.end.line - comment.loc.start.line;
                ***REMOVED***

                // avoid counting lines with inline comments twice
                if (comment.loc.start.line === lineNumTokenBefore) ***REMOVED***
                    numLinesComments--;
                ***REMOVED***

                if (comment.loc.end.line === node.loc.start.line) ***REMOVED***
                    numLinesComments--;
                ***REMOVED***
            ***REMOVED***);

            return numLinesComments;
        ***REMOVED***

        /**
         * Returns the line number of the token before the node that is passed in as an argument
         * @param ***REMOVED***ASTNode***REMOVED*** node - The node to use as the start of the calculation
         * @returns ***REMOVED***number***REMOVED*** Line number of the token before `node`
         * @private
         */
        function getLineNumberOfTokenBefore(node) ***REMOVED***
            const tokenBefore = sourceCode.getTokenBefore(node);
            let lineNumTokenBefore;

            /**
             * Global return (at the beginning of a script) is a special case.
             * If there is no token before `return`, then we expect no line
             * break before the return. Comments are allowed to occupy lines
             * before the global return, just no blank lines.
             * Setting lineNumTokenBefore to zero in that case results in the
             * desired behavior.
             */
            if (tokenBefore) ***REMOVED***
                lineNumTokenBefore = tokenBefore.loc.end.line;
            ***REMOVED*** else ***REMOVED***
                lineNumTokenBefore = 0; // global return at beginning of script
            ***REMOVED***

            return lineNumTokenBefore;
        ***REMOVED***

        /**
         * Checks whether node is preceded by a newline
         * @param ***REMOVED***ASTNode***REMOVED*** node - node to check
         * @returns ***REMOVED***boolean***REMOVED*** Whether or not the node is preceded by a newline
         * @private
         */
        function hasNewlineBefore(node) ***REMOVED***
            const lineNumNode = node.loc.start.line;
            const lineNumTokenBefore = getLineNumberOfTokenBefore(node);
            const commentLines = calcCommentLines(node, lineNumTokenBefore);

            return (lineNumNode - lineNumTokenBefore - commentLines) > 1;
        ***REMOVED***

        /**
         * Checks whether it is safe to apply a fix to a given return statement.
         *
         * The fix is not considered safe if the given return statement has leading comments,
         * as we cannot safely determine if the newline should be added before or after the comments.
         * For more information, see: https://github.com/eslint/eslint/issues/5958#issuecomment-222767211
         *
         * @param ***REMOVED***ASTNode***REMOVED*** node - The return statement node to check.
         * @returns ***REMOVED***boolean***REMOVED*** `true` if it can fix the node.
         * @private
         */
        function canFix(node) ***REMOVED***
            const leadingComments = sourceCode.getCommentsBefore(node);
            const lastLeadingComment = leadingComments[leadingComments.length - 1];
            const tokenBefore = sourceCode.getTokenBefore(node);

            if (leadingComments.length === 0) ***REMOVED***
                return true;
            ***REMOVED***

            /*
             * if the last leading comment ends in the same line as the previous token and
             * does not share a line with the `return` node, we can consider it safe to fix.
             * Example:
             * function a() ***REMOVED***
             *     var b; //comment
             *     return;
             * ***REMOVED***
             */
            if (lastLeadingComment.loc.end.line === tokenBefore.loc.end.line &&
                lastLeadingComment.loc.end.line !== node.loc.start.line) ***REMOVED***
                return true;
            ***REMOVED***

            return false;
        ***REMOVED***

        //--------------------------------------------------------------------------
        // Public
        //--------------------------------------------------------------------------

        return ***REMOVED***
            ReturnStatement(node) ***REMOVED***
                if (!isFirstNode(node) && !hasNewlineBefore(node)) ***REMOVED***
                    context.report(***REMOVED***
                        node,
                        messageId: "expected",
                        fix(fixer) ***REMOVED***
                            if (canFix(node)) ***REMOVED***
                                const tokenBefore = sourceCode.getTokenBefore(node);
                                const newlines = node.loc.start.line === tokenBefore.loc.end.line ? "\n\n" : "\n";

                                return fixer.insertTextBefore(node, newlines);
                            ***REMOVED***
                            return null;
                        ***REMOVED***
                    ***REMOVED***);
                ***REMOVED***
            ***REMOVED***
        ***REMOVED***;
    ***REMOVED***
***REMOVED***;

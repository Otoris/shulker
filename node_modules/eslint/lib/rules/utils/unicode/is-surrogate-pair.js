/**
 * @author Toru Nagashima <https://github.com/mysticatea>
 */
"use strict";

/**
 * Check whether given two characters are a surrogate pair.
 * @param ***REMOVED***number***REMOVED*** lead The code of the lead character.
 * @param ***REMOVED***number***REMOVED*** tail The code of the tail character.
 * @returns ***REMOVED***boolean***REMOVED*** `true` if the character pair is a surrogate pair.
 */
module.exports = function isSurrogatePair(lead, tail) ***REMOVED***
    return lead >= 0xD800 && lead < 0xDC00 && tail >= 0xDC00 && tail < 0xE000;
***REMOVED***;

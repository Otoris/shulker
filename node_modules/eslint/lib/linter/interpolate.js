/**
 * @fileoverview Interpolate keys from an object into a string with ***REMOVED******REMOVED*** ***REMOVED******REMOVED*** markers.
 * @author Jed Fox
 */

"use strict";

//------------------------------------------------------------------------------
// Public Interface
//------------------------------------------------------------------------------

module.exports = (text, data) => ***REMOVED***
    if (!data) ***REMOVED***
        return text;
    ***REMOVED***

    // Substitution content for any ***REMOVED******REMOVED*** ***REMOVED******REMOVED*** markers.
    return text.replace(/\***REMOVED***\***REMOVED***([^***REMOVED******REMOVED***]+?)\***REMOVED***\***REMOVED***/gu, (fullMatch, termWithWhitespace) => ***REMOVED***
        const term = termWithWhitespace.trim();

        if (term in data) ***REMOVED***
            return data[term];
        ***REMOVED***

        // Preserve old behavior: If parameter name not provided, don't replace it.
        return fullMatch;
    ***REMOVED***);
***REMOVED***;

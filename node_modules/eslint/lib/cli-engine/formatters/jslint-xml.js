/**
 * @fileoverview JSLint XML reporter
 * @author Ian Christian Myers
 */
"use strict";

const xmlEscape = require("../xml-escape");

//------------------------------------------------------------------------------
// Public Interface
//------------------------------------------------------------------------------

module.exports = function(results) ***REMOVED***

    let output = "";

    output += "<?xml version=\"1.0\" encoding=\"utf-8\"?>";
    output += "<jslint>";

    results.forEach(result => ***REMOVED***
        const messages = result.messages;

        output += `<file name="$***REMOVED***result.filePath***REMOVED***">`;

        messages.forEach(message => ***REMOVED***
            output += [
                `<issue line="$***REMOVED***message.line***REMOVED***"`,
                `char="$***REMOVED***message.column***REMOVED***"`,
                `evidence="$***REMOVED***xmlEscape(message.source || "")***REMOVED***"`,
                `reason="$***REMOVED***xmlEscape(message.message || "")***REMOVED***$***REMOVED***message.ruleId ? ` ($***REMOVED***message.ruleId***REMOVED***)` : ""***REMOVED***" />`
            ].join(" ");
        ***REMOVED***);

        output += "</file>";

    ***REMOVED***);

    output += "</jslint>";

    return output;
***REMOVED***;

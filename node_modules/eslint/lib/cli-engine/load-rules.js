/**
 * @fileoverview Module for loading rules from files and directories.
 * @author Michael Ficarra
 */

"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

const fs = require("fs"),
    path = require("path");

const rulesDirCache = ***REMOVED******REMOVED***;

//------------------------------------------------------------------------------
// Public Interface
//------------------------------------------------------------------------------

/**
 * Load all rule modules from specified directory.
 * @param ***REMOVED***string***REMOVED*** relativeRulesDir Path to rules directory, may be relative.
 * @param ***REMOVED***string***REMOVED*** cwd Current working directory
 * @returns ***REMOVED***Object***REMOVED*** Loaded rule modules.
 */
module.exports = function(relativeRulesDir, cwd) ***REMOVED***
    const rulesDir = path.resolve(cwd, relativeRulesDir);

    // cache will help performance as IO operation are expensive
    if (rulesDirCache[rulesDir]) ***REMOVED***
        return rulesDirCache[rulesDir];
    ***REMOVED***

    const rules = Object.create(null);

    fs.readdirSync(rulesDir).forEach(file => ***REMOVED***
        if (path.extname(file) !== ".js") ***REMOVED***
            return;
        ***REMOVED***
        rules[file.slice(0, -3)] = require(path.join(rulesDir, file));
    ***REMOVED***);
    rulesDirCache[rulesDir] = rules;

    return rules;
***REMOVED***;

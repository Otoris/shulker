// eslint-disable-next-line import/default
import validateConfig from '../dist/validateConfig';
// eslint-disable-next-line import/default
import validateStreamConfig from '../dist/validateStreamConfig';

const validate = ***REMOVED***
  'config.json': validateConfig,
  'streamConfig.json': validateStreamConfig
***REMOVED***;

/**
 * @param ***REMOVED***string***REMOVED*** schemaId
 * @param ***REMOVED***formatData~config***REMOVED*** config
 * @returns ***REMOVED***undefined***REMOVED***
 */
export default (schemaId, config = ***REMOVED******REMOVED***) => ***REMOVED***
  if (!validate[schemaId](config)) ***REMOVED***
    const errors = validate[schemaId].errors.map((error) => ***REMOVED***
      return ***REMOVED***
        dataPath: error.dataPath,
        message: error.message,
        params: error.params,
        schemaPath: error.schemaPath
      ***REMOVED***;
    ***REMOVED***);

    /* eslint-disable no-console */
    console.log('config', config);
    console.log('errors', errors);
    /* eslint-enable no-console */

    throw new Error('Invalid config.');
  ***REMOVED***
***REMOVED***;

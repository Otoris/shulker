import calculateCellWidthIndex from './calculateCellWidthIndex';

/**
 * Produces an array of values that describe the largest value length (width) in every column.
 *
 * @param ***REMOVED***Array[]***REMOVED*** rows
 * @returns ***REMOVED***number[]***REMOVED***
 */
export default (rows) => ***REMOVED***
  if (!rows[0]) ***REMOVED***
    throw new Error('Dataset must have at least one row.');
  ***REMOVED***

  const columns = new Array(rows[0].length).fill(0);

  rows.forEach((row) => ***REMOVED***
    const columnWidthIndex = calculateCellWidthIndex(row);

    columnWidthIndex.forEach((valueWidth, index0) => ***REMOVED***
      if (columns[index0] < valueWidth) ***REMOVED***
        columns[index0] = valueWidth;
      ***REMOVED***
    ***REMOVED***);
  ***REMOVED***);

  return columns;
***REMOVED***;

import slice from 'slice-ansi';
import stringWidth from 'string-width';

/**
 * @param ***REMOVED***string***REMOVED*** input
 * @param ***REMOVED***number***REMOVED*** size
 * @returns ***REMOVED***Array***REMOVED***
 */
export default (input, size) => ***REMOVED***
  let subject;

  subject = input;

  const chunks = [];

  // https://regex101.com/r/gY5kZ1/1
  const re = new RegExp('(^.***REMOVED***1,' + size + '***REMOVED***(\\s+|$))|(^.***REMOVED***1,' + (size - 1) + '***REMOVED***(\\\\|/|_|\\.|,|;|-))');

  do ***REMOVED***
    let chunk;

    chunk = subject.match(re);

    if (chunk) ***REMOVED***
      chunk = chunk[0];

      subject = slice(subject, stringWidth(chunk));

      chunk = chunk.trim();
    ***REMOVED*** else ***REMOVED***
      chunk = slice(subject, 0, size);
      subject = slice(subject, size);
    ***REMOVED***

    chunks.push(chunk);
  ***REMOVED*** while (stringWidth(subject));

  return chunks;
***REMOVED***;

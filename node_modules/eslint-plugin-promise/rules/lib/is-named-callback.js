'use strict'

let callbacks = ['done', 'cb', 'callback', 'next']

module.exports = function isNamedCallback(potentialCallbackName, exceptions) ***REMOVED***
  for (let i = 0; i < exceptions.length; i++) ***REMOVED***
    callbacks = callbacks.filter(item => ***REMOVED***
      return item !== exceptions[i]
    ***REMOVED***)
  ***REMOVED***
  return callbacks.some(trueCallbackName => ***REMOVED***
    return potentialCallbackName === trueCallbackName
  ***REMOVED***)
***REMOVED***

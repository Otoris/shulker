'use strict'

const getDocsUrl = require('./lib/get-docs-url')

module.exports = ***REMOVED***
  meta: ***REMOVED***
    docs: ***REMOVED***
      url: getDocsUrl('param-names')
    ***REMOVED***,
    fixable: 'code'
  ***REMOVED***,
  create(context) ***REMOVED***
    return ***REMOVED***
      NewExpression(node) ***REMOVED***
        if (node.callee.name === 'Promise' && node.arguments.length === 1) ***REMOVED***
          const params = node.arguments[0].params

          if (!params || !params.length) ***REMOVED***
            return
          ***REMOVED***

          if (
            params[0].name !== 'resolve' ||
            (params[1] && params[1].name !== 'reject')
          ) ***REMOVED***
            context.report(***REMOVED***
              node,
              message:
                'Promise constructor parameters must be named resolve, reject'
            ***REMOVED***)
          ***REMOVED***
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***
***REMOVED***

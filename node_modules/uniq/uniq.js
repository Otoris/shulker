"use strict"

function unique_pred(list, compare) ***REMOVED***
  var ptr = 1
    , len = list.length
    , a=list[0], b=list[0]
  for(var i=1; i<len; ++i) ***REMOVED***
    b = a
    a = list[i]
    if(compare(a, b)) ***REMOVED***
      if(i === ptr) ***REMOVED***
        ptr++
        continue
      ***REMOVED***
      list[ptr++] = a
    ***REMOVED***
  ***REMOVED***
  list.length = ptr
  return list
***REMOVED***

function unique_eq(list) ***REMOVED***
  var ptr = 1
    , len = list.length
    , a=list[0], b = list[0]
  for(var i=1; i<len; ++i, b=a) ***REMOVED***
    b = a
    a = list[i]
    if(a !== b) ***REMOVED***
      if(i === ptr) ***REMOVED***
        ptr++
        continue
      ***REMOVED***
      list[ptr++] = a
    ***REMOVED***
  ***REMOVED***
  list.length = ptr
  return list
***REMOVED***

function unique(list, compare, sorted) ***REMOVED***
  if(list.length === 0) ***REMOVED***
    return list
  ***REMOVED***
  if(compare) ***REMOVED***
    if(!sorted) ***REMOVED***
      list.sort(compare)
    ***REMOVED***
    return unique_pred(list, compare)
  ***REMOVED***
  if(!sorted) ***REMOVED***
    list.sort()
  ***REMOVED***
  return unique_eq(list)
***REMOVED***

module.exports = unique

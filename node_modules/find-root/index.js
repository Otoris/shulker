var path = require('path')
var fs = require('fs')

function defaultCheck (dir) ***REMOVED***
  return fs.existsSync(path.join(dir, 'package.json'))
***REMOVED***

function findRoot (start, check) ***REMOVED***
  start = start || module.parent.filename
  check = check || defaultCheck

  if (typeof start === 'string') ***REMOVED***
    if (start[start.length - 1] !== path.sep) ***REMOVED***
      start += path.sep
    ***REMOVED***
    start = start.split(path.sep)
  ***REMOVED***
  if (!start.length) ***REMOVED***
    throw new Error('package.json not found in path')
  ***REMOVED***
  start.pop()
  var dir = start.join(path.sep)
  try ***REMOVED***
    if (check(dir)) ***REMOVED***
      return dir
    ***REMOVED***
  ***REMOVED*** catch (e) ***REMOVED******REMOVED***
  return findRoot(start, check)
***REMOVED***

module.exports = findRoot

var regexCreator = require('emoji-regex')

var emojiRegex = regexCreator()

function emojiStrip (string) ***REMOVED***
  return string.replace(emojiRegex, '')
***REMOVED***

module.exports = emojiStrip


var Match = require ('../match');

/**
 * Charset recognizer for UTF-8
 */
module.exports = function() ***REMOVED***
  this.name = function() ***REMOVED***
    return 'UTF-8';
  ***REMOVED***;
  this.match = function(det) ***REMOVED***

    var hasBOM = false,
      numValid = 0,
      numInvalid = 0,
      input = det.fRawInput,
      trailBytes = 0,
      confidence;

    if (det.fRawLength >= 3 &&
      (input[0] & 0xff) == 0xef && (input[1] & 0xff) == 0xbb && (input[2] & 0xff) == 0xbf) ***REMOVED***
      hasBOM = true;
    ***REMOVED***

    // Scan for multi-byte sequences
    for (var i = 0; i < det.fRawLength; i++) ***REMOVED***
      var b = input[i];
      if ((b & 0x80) == 0)
        continue; // ASCII

      // Hi bit on char found.  Figure out how long the sequence should be
      if ((b & 0x0e0) == 0x0c0) ***REMOVED***
        trailBytes = 1;
      ***REMOVED*** else if ((b & 0x0f0) == 0x0e0) ***REMOVED***
        trailBytes = 2;
      ***REMOVED*** else if ((b & 0x0f8) == 0xf0) ***REMOVED***
        trailBytes = 3;
      ***REMOVED*** else ***REMOVED***
        numInvalid++;
        if (numInvalid > 5)
          break;
        trailBytes = 0;
      ***REMOVED***

      // Verify that we've got the right number of trail bytes in the sequence
      for (;;) ***REMOVED***
        i++;
        if (i >= det.fRawLength)
          break;

        if ((input[i] & 0xc0) != 0x080) ***REMOVED***
          numInvalid++;
          break;
        ***REMOVED***
        if (--trailBytes == 0) ***REMOVED***
          numValid++;
          break;
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***

    // Cook up some sort of confidence score, based on presense of a BOM
    //    and the existence of valid and/or invalid multi-byte sequences.
    confidence = 0;
    if (hasBOM && numInvalid == 0)
      confidence = 100;
    else if (hasBOM && numValid > numInvalid * 10)
      confidence = 80;
    else if (numValid > 3 && numInvalid == 0)
      confidence = 100;
    else if (numValid > 0 && numInvalid == 0)
      confidence = 80;
    else if (numValid == 0 && numInvalid == 0)
      // Plain ASCII.
      confidence = 10;
    else if (numValid > numInvalid * 10)
      // Probably corruput utf-8 data.  Valid sequences aren't likely by chance.
      confidence = 25;
    else
      return null

    return new Match(det, this, confidence);
  ***REMOVED***;
***REMOVED***;

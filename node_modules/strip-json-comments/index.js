'use strict';
const singleComment = Symbol('singleComment');
const multiComment = Symbol('multiComment');
const stripWithoutWhitespace = () => '';
const stripWithWhitespace = (string, start, end) => string.slice(start, end).replace(/\S/g, ' ');

const isEscaped = (jsonString, quotePosition) => ***REMOVED***
	let index = quotePosition - 1;
	let backslashCount = 0;

	while (jsonString[index] === '\\') ***REMOVED***
		index -= 1;
		backslashCount += 1;
	***REMOVED***

	return Boolean(backslashCount % 2);
***REMOVED***;

module.exports = (jsonString, options = ***REMOVED******REMOVED***) => ***REMOVED***
	const strip = options.whitespace === false ? stripWithoutWhitespace : stripWithWhitespace;

	let insideString = false;
	let insideComment = false;
	let offset = 0;
	let result = '';

	for (let i = 0; i < jsonString.length; i++) ***REMOVED***
		const currentCharacter = jsonString[i];
		const nextCharacter = jsonString[i + 1];

		if (!insideComment && currentCharacter === '"') ***REMOVED***
			const escaped = isEscaped(jsonString, i);
			if (!escaped) ***REMOVED***
				insideString = !insideString;
			***REMOVED***
		***REMOVED***

		if (insideString) ***REMOVED***
			continue;
		***REMOVED***

		if (!insideComment && currentCharacter + nextCharacter === '//') ***REMOVED***
			result += jsonString.slice(offset, i);
			offset = i;
			insideComment = singleComment;
			i++;
		***REMOVED*** else if (insideComment === singleComment && currentCharacter + nextCharacter === '\r\n') ***REMOVED***
			i++;
			insideComment = false;
			result += strip(jsonString, offset, i);
			offset = i;
			continue;
		***REMOVED*** else if (insideComment === singleComment && currentCharacter === '\n') ***REMOVED***
			insideComment = false;
			result += strip(jsonString, offset, i);
			offset = i;
		***REMOVED*** else if (!insideComment && currentCharacter + nextCharacter === '/*') ***REMOVED***
			result += jsonString.slice(offset, i);
			offset = i;
			insideComment = multiComment;
			i++;
			continue;
		***REMOVED*** else if (insideComment === multiComment && currentCharacter + nextCharacter === '*/') ***REMOVED***
			i++;
			insideComment = false;
			result += strip(jsonString, offset, i + 1);
			offset = i + 1;
			continue;
		***REMOVED***
	***REMOVED***

	return result + (insideComment ? strip(jsonString.slice(offset)) : jsonString.slice(offset));
***REMOVED***;

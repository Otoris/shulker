'use strict';

var Type = require('../type');

var _hasOwnProperty = Object.prototype.hasOwnProperty;

function resolveYamlSet(data) ***REMOVED***
  if (data === null) return true;

  var key, object = data;

  for (key in object) ***REMOVED***
    if (_hasOwnProperty.call(object, key)) ***REMOVED***
      if (object[key] !== null) return false;
    ***REMOVED***
  ***REMOVED***

  return true;
***REMOVED***

function constructYamlSet(data) ***REMOVED***
  return data !== null ? data : ***REMOVED******REMOVED***;
***REMOVED***

module.exports = new Type('tag:yaml.org,2002:set', ***REMOVED***
  kind: 'mapping',
  resolve: resolveYamlSet,
  construct: constructYamlSet
***REMOVED***);

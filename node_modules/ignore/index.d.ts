type Pathname = string

interface TestResult ***REMOVED***
  ignored: boolean
  unignored: boolean
***REMOVED***

export interface Ignore ***REMOVED***
  /**
   * Adds a rule rules to the current manager.
   * @param  ***REMOVED***string | Ignore***REMOVED*** pattern
   * @returns IgnoreBase
   */
  add(pattern: string | Ignore): this
  /**
   * Adds several rules to the current manager.
   * @param  ***REMOVED***string[]***REMOVED*** patterns
   * @returns IgnoreBase
   */
  add(patterns: (string | Ignore)[]): this

  /**
   * Filters the given array of pathnames, and returns the filtered array.
   * NOTICE that each path here should be a relative path to the root of your repository.
   * @param paths the array of paths to be filtered.
   * @returns The filtered array of paths
   */
  filter(pathnames: Pathname[]): Pathname[]
  /**
   * Creates a filter function which could filter
   * an array of paths with Array.prototype.filter.
   */
  createFilter(): (pathname: Pathname) => boolean

  /**
   * Returns Boolean whether pathname should be ignored.
   * @param  ***REMOVED***string***REMOVED*** pathname a path to check
   * @returns boolean
   */
  ignores(pathname: Pathname): boolean

  /**
   * Returns whether pathname should be ignored or unignored
   * @param  ***REMOVED***string***REMOVED*** pathname a path to check
   * @returns TestResult
   */
  test(pathname: Pathname): TestResult
***REMOVED***

interface Options ***REMOVED***
  ignorecase?: boolean
***REMOVED***

/**
 * Creates new ignore manager.
 */
declare function ignore(options?: Options): Ignore

declare namespace ignore ***REMOVED***
  export function isPathValid (pathname: string): boolean
***REMOVED***

export default ignore

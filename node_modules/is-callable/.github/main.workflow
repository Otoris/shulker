workflow "Autorebase branch on merge commits" ***REMOVED***
	on = "push"
	resolves = ["rebase"]
***REMOVED***

workflow "Autorebase PR on merge commits" ***REMOVED***
	on = "pull_request"
	resolves = ["rebase"]
***REMOVED***

 action "rebase" ***REMOVED***
	uses = "ljharb/rebase@latest"
	secrets = ["GITHUB_TOKEN"]
***REMOVED***

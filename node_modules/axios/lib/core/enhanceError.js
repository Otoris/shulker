'use strict';

/**
 * Update an Error with the specified config, error code, and response.
 *
 * @param ***REMOVED***Error***REMOVED*** error The error to update.
 * @param ***REMOVED***Object***REMOVED*** config The config.
 * @param ***REMOVED***string***REMOVED*** [code] The error code (for example, 'ECONNABORTED').
 * @param ***REMOVED***Object***REMOVED*** [request] The request.
 * @param ***REMOVED***Object***REMOVED*** [response] The response.
 * @returns ***REMOVED***Error***REMOVED*** The error.
 */
module.exports = function enhanceError(error, config, code, request, response) ***REMOVED***
  error.config = config;
  if (code) ***REMOVED***
    error.code = code;
  ***REMOVED***

  error.request = request;
  error.response = response;
  error.isAxiosError = true;

  error.toJSON = function() ***REMOVED***
    return ***REMOVED***
      // Standard
      message: this.message,
      name: this.name,
      // Microsoft
      description: this.description,
      number: this.number,
      // Mozilla
      fileName: this.fileName,
      lineNumber: this.lineNumber,
      columnNumber: this.columnNumber,
      stack: this.stack,
      // Axios
      config: this.config,
      code: this.code
    ***REMOVED***;
  ***REMOVED***;
  return error;
***REMOVED***;

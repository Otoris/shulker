'use strict';

var utils = require('./../utils');

/**
 * Transform the data for a request or a response
 *
 * @param ***REMOVED***Object|String***REMOVED*** data The data to be transformed
 * @param ***REMOVED***Array***REMOVED*** headers The headers for the request or response
 * @param ***REMOVED***Array|Function***REMOVED*** fns A single function or Array of functions
 * @returns ***REMOVED*******REMOVED*** The resulting transformed data
 */
module.exports = function transformData(data, headers, fns) ***REMOVED***
  /*eslint no-param-reassign:0*/
  utils.forEach(fns, function transform(fn) ***REMOVED***
    data = fn(data, headers);
  ***REMOVED***);

  return data;
***REMOVED***;

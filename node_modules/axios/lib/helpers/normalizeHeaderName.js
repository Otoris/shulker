'use strict';

var utils = require('../utils');

module.exports = function normalizeHeaderName(headers, normalizedName) ***REMOVED***
  utils.forEach(headers, function processHeader(value, name) ***REMOVED***
    if (name !== normalizedName && name.toUpperCase() === normalizedName.toUpperCase()) ***REMOVED***
      headers[normalizedName] = value;
      delete headers[name];
    ***REMOVED***
  ***REMOVED***);
***REMOVED***;

exports = ***REMOVED***
  parse: (str) => ***REMOVED***
    const parsed = ***REMOVED******REMOVED***;
    for (const [k, v] of new Window.URLSearchParams(str).entries())
      parsed[k] = v;
    return parsed;
  ***REMOVED***,
  stringify: (obj) => new window.URLSearchParams(obj).toString(),
***REMOVED***;

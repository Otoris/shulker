/**
 * @fileoverview Utility functions for propWrapperFunctions setting
 */

'use strict';

/** TODO: type ***REMOVED***(string | ***REMOVED*** name: string, linkAttribute: string ***REMOVED***)[]***REMOVED*** */
/** @type ***REMOVED***any***REMOVED*** */
const DEFAULT_LINK_COMPONENTS = ['a'];
const DEFAULT_LINK_ATTRIBUTE = 'href';

function getLinkComponents(context) ***REMOVED***
  const settings = context.settings || ***REMOVED******REMOVED***;
  const linkComponents = /** @type ***REMOVED***typeof DEFAULT_LINK_COMPONENTS***REMOVED*** */ (
    DEFAULT_LINK_COMPONENTS.concat(settings.linkComponents || [])
  );
  return new Map(linkComponents.map((value) => ***REMOVED***
    if (typeof value === 'string') ***REMOVED***
      return [value, DEFAULT_LINK_ATTRIBUTE];
    ***REMOVED***
    return [value.name, value.linkAttribute];
  ***REMOVED***));
***REMOVED***

module.exports = ***REMOVED***
  getLinkComponents
***REMOVED***;

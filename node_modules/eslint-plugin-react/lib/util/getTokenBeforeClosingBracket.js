'use strict';

/**
 * Find the token before the closing bracket.
 * @param ***REMOVED***ASTNode***REMOVED*** node - The JSX element node.
 * @returns ***REMOVED***Token***REMOVED*** The token before the closing bracket.
 */
function getTokenBeforeClosingBracket(node) ***REMOVED***
  const attributes = node.attributes;
  if (attributes.length === 0) ***REMOVED***
    return node.name;
  ***REMOVED***
  return attributes[attributes.length - 1];
***REMOVED***

module.exports = getTokenBeforeClosingBracket;

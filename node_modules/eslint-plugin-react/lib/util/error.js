'use strict';

/**
 * Logs out a message if there is no format option set.
 * @param ***REMOVED***String***REMOVED*** message - Message to log.
 */
function error(message) ***REMOVED***
  if (!/=-(f|-format)=/.test(process.argv.join('='))) ***REMOVED***
    // eslint-disable-next-line no-console
    console.error(message);
  ***REMOVED***
***REMOVED***

module.exports = error;

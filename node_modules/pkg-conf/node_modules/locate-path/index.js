'use strict';
const path = require('path');
const pathExists = require('path-exists');
const pLocate = require('p-locate');

module.exports = (iterable, options) => ***REMOVED***
	options = Object.assign(***REMOVED***
		cwd: process.cwd()
	***REMOVED***, options);

	return pLocate(iterable, el => pathExists(path.resolve(options.cwd, el)), options);
***REMOVED***;

module.exports.sync = (iterable, options) => ***REMOVED***
	options = Object.assign(***REMOVED***
		cwd: process.cwd()
	***REMOVED***, options);

	for (const el of iterable) ***REMOVED***
		if (pathExists.sync(path.resolve(options.cwd, el))) ***REMOVED***
			return el;
		***REMOVED***
	***REMOVED***
***REMOVED***;

var path = require('path');
var test = require('tape');
var resolve = require('../');

test('dotdot', function (t) ***REMOVED***
    t.plan(4);
    var dir = path.join(__dirname, '/dotdot/abc');

    resolve('..', ***REMOVED*** basedir: dir ***REMOVED***, function (err, res, pkg) ***REMOVED***
        t.ifError(err);
        t.equal(res, path.join(__dirname, 'dotdot/index.js'));
    ***REMOVED***);

    resolve('.', ***REMOVED*** basedir: dir ***REMOVED***, function (err, res, pkg) ***REMOVED***
        t.ifError(err);
        t.equal(res, path.join(dir, 'index.js'));
    ***REMOVED***);
***REMOVED***);

test('dotdot sync', function (t) ***REMOVED***
    t.plan(2);
    var dir = path.join(__dirname, '/dotdot/abc');

    var a = resolve.sync('..', ***REMOVED*** basedir: dir ***REMOVED***);
    t.equal(a, path.join(__dirname, 'dotdot/index.js'));

    var b = resolve.sync('.', ***REMOVED*** basedir: dir ***REMOVED***);
    t.equal(b, path.join(dir, 'index.js'));
***REMOVED***);

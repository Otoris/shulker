var test = require('tape');
var resolve = require('../');
var path = require('path');

test('shadowed core modules still return core module', function (t) ***REMOVED***
    t.plan(2);

    resolve('util', ***REMOVED*** basedir: path.join(__dirname, 'shadowed_core') ***REMOVED***, function (err, res) ***REMOVED***
        t.ifError(err);
        t.equal(res, 'util');
    ***REMOVED***);
***REMOVED***);

test('shadowed core modules still return core module [sync]', function (t) ***REMOVED***
    t.plan(1);

    var res = resolve.sync('util', ***REMOVED*** basedir: path.join(__dirname, 'shadowed_core') ***REMOVED***);

    t.equal(res, 'util');
***REMOVED***);

test('shadowed core modules return shadow when appending `/`', function (t) ***REMOVED***
    t.plan(2);

    resolve('util/', ***REMOVED*** basedir: path.join(__dirname, 'shadowed_core') ***REMOVED***, function (err, res) ***REMOVED***
        t.ifError(err);
        t.equal(res, path.join(__dirname, 'shadowed_core/node_modules/util/index.js'));
    ***REMOVED***);
***REMOVED***);

test('shadowed core modules return shadow when appending `/` [sync]', function (t) ***REMOVED***
    t.plan(1);

    var res = resolve.sync('util/', ***REMOVED*** basedir: path.join(__dirname, 'shadowed_core') ***REMOVED***);

    t.equal(res, path.join(__dirname, 'shadowed_core/node_modules/util/index.js'));
***REMOVED***);


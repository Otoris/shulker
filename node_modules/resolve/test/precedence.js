var path = require('path');
var test = require('tape');
var resolve = require('../');

test('precedence', function (t) ***REMOVED***
    t.plan(3);
    var dir = path.join(__dirname, 'precedence/aaa');

    resolve('./', ***REMOVED*** basedir: dir ***REMOVED***, function (err, res, pkg) ***REMOVED***
        t.ifError(err);
        t.equal(res, path.join(dir, 'index.js'));
        t.equal(pkg.name, 'resolve');
    ***REMOVED***);
***REMOVED***);

test('./ should not load $***REMOVED***dir***REMOVED***.js', function (t) ***REMOVED*** // eslint-disable-line no-template-curly-in-string
    t.plan(1);
    var dir = path.join(__dirname, 'precedence/bbb');

    resolve('./', ***REMOVED*** basedir: dir ***REMOVED***, function (err, res, pkg) ***REMOVED***
        t.ok(err);
    ***REMOVED***);
***REMOVED***);
